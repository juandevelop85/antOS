# T31.15 · Descomposición de Módulos de Gran Tamaño

> **Estado:** ✅ Completado
> **Fase:** Fase 31 (Auditoría de Código: Corrección y Endurecimiento)
> **Prioridad:** 🟢 Mejora
> **Fecha:** September 2026
> **Dependencias:** T22.1 (modularización de `antosd`), T22.2 (descomposición de `antos-protocolo`), T31.13 (cobertura de tests).
> **Origen:** Auditoría de código de September 2026.

## Descripción

T22.1 y T22.2 partieron `antosd` y `antos-protocolo` en submódulos temáticos, y
el resultado se nota: la estructura de directorios es legible y el reparto por
áreas tiene sentido. Pero varios ficheros han vuelto a crecer por encima del
punto en que caben en la cabeza de quien los lee.

### Ficheros por encima de 1500 líneas

| Fichero | Líneas | Observación |
| :--- | ---: | :--- |
| `system/antosd/src/cli/commands/tools.rs` | 3037 | Además, sin ningún test (T31.13). |
| `system/antosd/src/exec/mod.rs` | 2935 | Despacho de ejecución de capacidades. |
| `system/antosd/src/planner/local.rs` | 1986 | Concentra la mayoría de los tests del catálogo de capacidades. |
| `system/antosd/src/pkg.rs` | 1681 | Gestor de paquetes `antpkg`. |
| `system/antosd/src/ipc.rs` | 1625 | Despacho IPC; ver también T31.8. |
| `system/antosd/src/cli/commands/system.rs` | 1532 | Sin tests. |
| `kernel/src/drivers/usb/xhci/mod.rs` | 1401 | Ya tiene submódulos (`ring`, `context`, `registers`); el `mod.rs` es lo que queda por repartir. |

El árbol suma unas 43 000 líneas en `antosd`, 24 000 en el kernel, 7600 en el
protocolo y 1350 en la barra. El problema no es el total, es la concentración:
`tools.rs` por sí solo pesa más que todo el crate del protocolo antes de T22.2.

### Por qué merece la pena

- Un fichero de 3000 líneas hace que cualquier `git diff` sea difícil de
  revisar y multiplica los conflictos cuando dos ramas lo tocan.
- Es donde se acumulan las funciones con demasiados parámetros que clippy ya
  señala (`too_many_arguments`).
- La falta de tests en `tools.rs` y `system.rs` (T31.13) es en parte
  consecuencia del tamaño: no hay unidades pequeñas que probar.

## Alcance Técnico

1. **`cli/commands/tools.rs`.** Partir por familia de subcomando, siguiendo el
   patrón que ya usan `app.rs`, `pkg.rs` y `vfs.rs`. Extraer primero el
   análisis de argumentos, que es lo que T31.13 necesita poder probar.
2. **`exec/mod.rs`.** Ya existen `exec/{fs,git,sandbox,service}.rs`; mover el
   resto del despacho a submódulos por área de capacidad y dejar en `mod.rs`
   solo la tabla de despacho.
3. **`pkg.rs`.** Separar recetas, resolución y ejecución de instalación.
4. **`ipc.rs`.** Separar el transporte (aceptar, enmarcar, leer, escribir) del
   despacho de peticiones. La corrección de T31.8 toca el transporte, así que
   conviene coordinar ambos tickets.
5. **`planner/local.rs`.** Extraer los tests a `planner/local/tests.rs`, que
   por sí solo reduce el fichero de forma notable.
6. **Umbral acordado.** Fijar una guía —por ejemplo 800 líneas— y anotarla en
   las reglas de desarrollo como señal de revisión, no como prohibición.

## Criterios de Aceptación

- Ningún fichero `.rs` de `system/` supera las 1500 líneas.
- La descomposición no cambia ninguna API pública ni el comportamiento
  observable: `cargo test --workspace` pasa con el mismo número de tests antes
  y después.
- Cada extracción va en su propio commit, sin mezclar movimiento de código con
  cambios de lógica.
- El umbral de tamaño queda documentado en
  `.agents/rules/antos-development.md`.
- `cargo clippy --workspace --all-targets -- -D warnings` sin avisos.

## Estado de Verificación

El criterio de aceptación central («ningún fichero `.rs` de `system/`
supera las 1500 líneas») era, a la fecha de esta pasada, más ambicioso de
lo que el propio ticket documentaba: los ficheros habían seguido
creciendo desde que se escribió la tabla, y aparecieron dos casos nuevos
por encima del umbral que la tabla no menciona
(`system/protocolo/src/tests.rs`, `system/barra/src/main.rs`). Se
priorizaron las extracciones **seguras** — código que se mueve sin tocar
ningún `match` de despacho de una sola función gigante — y se dejaron
documentadas, no forzadas a medias, las que exigen reescribir lógica de
despacho real.

### 1. Completado, en siete commits de movimiento puro

- `cli/commands/tools.rs` (3592 → 0, repartido en 11 ficheros bajo
  `cli/commands/tools/`, todos <800 líneas). Alcance punto 1, completo.
- `cli/commands/system.rs` (2065 → 0, repartido en 7 ficheros bajo
  `cli/commands/system/`, todos <800 líneas). No estaba en la tabla del
  ticket, pero T31.13 ya lo había señalado sin tests hasta esa misma
  ronda, y crecía por el mismo motivo que `tools.rs`.
- `pkg.rs` (1963 → 0, repartido en `pkg/{mod,recipes,desktop,lifecycle,
  tests}.rs`, el más grande con 676 líneas). Alcance punto 3, completo:
  "recetas, resolución y ejecución de instalación" son, literalmente,
  `recipes.rs`, `recipes.rs` (resolución vive junto al parseo) y
  `lifecycle.rs`.
- `planner/local.rs` (2858 → `planner/local/mod.rs` 1837 líneas +
  `planner/local/tests.rs` 1023 líneas). Alcance punto 5, hecho tal como
  lo pedía el propio ticket ("extraer los tests... que por sí solo reduce
  el fichero de forma notable") — **no** deja `mod.rs` por debajo de 1500,
  algo que el ticket nunca prometía para este punto.
- `protocolo/src/tests.rs` (2211 → 0, repartido en 4 bloques bajo
  `protocolo/src/tests/`, todos <650 líneas). No estaba en la tabla del
  ticket —creció por encima de 1500 después de escrita—, pero el criterio
  de aceptación es incondicional para `system/`, así que se aplicó el
  mismo trato: 43 tests independientes entre sí, repartidos en bloques
  consecutivos sin necesidad de agruparlos por tema.
- `exec/mod.rs` (3699 → 3047): se extrajeron `Change` (513 líneas) y
  `PendingChanges`/`read_with_pending` (163 líneas) a ficheros propios —
  las dos únicas partes autocontenidas, sin ningún `match` de despacho.
- `ipc.rs` (2607 → `ipc/mod.rs` 2417 + `ipc/transport.rs` 216): se separó
  el transporte del socket (`send`/`receive`/`Received`, `SocketHandler`,
  `bind_socket*`, `is_idle_timeout`, `serve`) del despacho, exactamente
  como pide el Alcance Técnico punto 4 ("separar el transporte... del
  despacho de peticiones"). Verificado además con el test de integración
  `tests/desktop_ipc.rs`, que habla con el demonio real por el socket de
  verdad tras la extracción.

Cada uno de los siete es su propio commit, y cada uno se verificó por
separado antes del siguiente: `cargo test --workspace` con el mismo
número exacto de tests antes y después (434, sin excepción, en los siete),
`cargo clippy --workspace --all-targets -- -D warnings` limpio, `cargo
fmt -- --check` limpio, y para el commit de `ipc.rs` en particular el test
de integración sobre el socket real.

### 2. Diferido, documentado explícitamente

Cuatro ficheros siguen por encima de 1500 líneas al cerrar este ticket:

- **`exec/mod.rs` (3047 líneas).** Lo que queda son `changes_for` (~1000
  líneas) y `apply` (~1800 líneas): dos funciones únicas que despachan
  sobre `Change` con un `match` de decenas de brazos cada una. Partirlas
  en submódulos por área de capacidad (Alcance punto 2) exige reescribir
  cada brazo como una llamada a una función aparte que reciba exactamente
  los mismos datos y produzca exactamente el mismo efecto — un trabajo de
  volumen y riesgo comparable al de un ticket propio, no una tarde de
  movimiento mecánico. Intentarlo a medias, sin poder revisar cada brazo
  con calma, es más probable que introduzca una regresión de comportamiento
  silenciosa que ganar legibilidad real.
- **`ipc/mod.rs` (2417 líneas).** Mismo problema, mismo motivo: lo que
  queda es `handle_connection` (~1115 líneas, el despacho de peticiones
  IPC) y `remote_intent` (~1060 líneas, el cliente CLI). Alcance punto 4
  pedía separar el transporte del despacho — hecho — pero no prometía que
  el despacho en sí bajara de 1500 líneas, y partirlo tiene el mismo perfil
  de riesgo que `apply`.
- **`planner/local/mod.rs` (1837 líneas).** Un único `impl Planner for
  LocalPlanner { fn plan(...) }` con un despachador extenso de lenguaje
  natural a capacidades. La extracción de tests (Alcance punto 5) ya está
  hecha; partir `plan()` en sí no estaba en el alcance de este ticket.
- **`system/barra/src/main.rs` (1528 líneas, GTK4/Wayland).** Apenas por
  encima del umbral, y sin poder compilarse ni comprobarse en esta máquina
  (macOS, sin `gtk4-layer-shell` — mismo hueco documentado en T31.10 y
  T31.13). Tocar un fichero de este tamaño sin poder compilarlo aquí sería
  especular a ciegas; se deja para una sesión con acceso al runner de
  Linux.

### 3. Verificación ejecutada (estado final)

- `cargo check --workspace`: ✅ verde.
- `cargo test --workspace`: ✅ verde, 434 tests — el mismo número que
  antes de empezar este ticket, en cada uno de los siete commits.
- `cargo clippy --workspace --all-targets -- -D warnings`: ✅ limpio.
- `cargo clippy -p antosd --all-targets -- -D clippy::unwrap_used -D
  clippy::expect_used` (puerta T31.7): ✅ limpio.
- `cargo fmt --manifest-path system/antosd/Cargo.toml -- --check` y el
  equivalente para `system/protocolo`: ✅ limpio.
- `.agents/scripts/check-spanish-identifiers.py`: ✅ 0 coincidencias.

### Criterios de Aceptación — estado

- ⚠️ «Ningún fichero `.rs` de `system/` supera las 1500 líneas.» — de los
  6+ ficheros originalmente por encima del umbral (más los 2 descubiertos
  en el camino), 5 quedan resueltos por completo; 4 siguen por encima,
  documentados arriba con la razón concreta de por qué no se forzaron.
- ✅ «La descomposición no cambia ninguna API pública ni el comportamiento
  observable: `cargo test --workspace` pasa con el mismo número de tests
  antes y después.» — verificado en los siete commits, sin excepción.
- ✅ «Cada extracción va en su propio commit, sin mezclar movimiento de
  código con cambios de lógica.» — siete commits de movimiento puro, más
  uno de documentación de la regla.
- ✅ «El umbral de tamaño queda documentado en
  `.agents/rules/antos-development.md`.»
- ✅ «`cargo clippy --workspace --all-targets -- -D warnings` sin avisos.»
