# T31.3 · Desbordamientos Aritméticos en el Cargador ELF y en tarfs del Kernel

> **Estado:** ✅ Completado
> **Fase:** Fase 31 (Auditoría de Código: Corrección y Endurecimiento)
> **Prioridad:** 🔴 Crítica
> **Fecha:** September 2026
> **Dependencias:** T26.4 (Cargador ELF64 e initramfs tarfs), T29.2 (ejecución de ELF externos).
> **Origen:** Auditoría de código de September 2026.

## Descripción

`kernel/src/elf.rs` valida los límites de una imagen ELF con sumas y
multiplicaciones sin comprobar. El perfil `release` del kernel no lleva
`overflow-checks`, así que ahí los enteros dan la vuelta en silencio: una imagen
manipulada puede pasar la validación y provocar lecturas fuera del búfer en un
contexto sin protección de memoria. El mismo patrón está en `fs/tarfs.rs`.

### Sitios afectados en `elf.rs`

| Línea | Expresión | Riesgo |
| :--- | :--- | :--- |
| 98 | `ph_offset + ph_count * ph_size > image.len()` | `ph_count` y `ph_size` son `u16` del fichero; el producto y la suma pueden dar la vuelta y validar una tabla de cabeceras inexistente. |
| 108 | `entry_offset + core::mem::size_of::<ProgramHeader>() > image.len()` | `entry_offset` acumula el mismo producto sin comprobar. |
| 121 | `file_off + file_sz > image.len()` | Ambos vienen de campos `u64` del segmento; al desbordar, la comprobación deja pasar un `copy_nonoverlapping` fuera de la imagen. |
| 217 | `from + length > image.len()` | Mismo caso en `load_segment` de x86_64. |

`parse_elf` es la única barrera antes de los `read_unaligned` y
`copy_nonoverlapping` de `load` y `load_aarch64`, que releen las cabeceras sin
volver a validarlas. Si la barrera cede, el cargador copia desde fuera de la
imagen.

### Defecto adicional · Mantenimiento de caché incompleto en AArch64

`load_aarch64` (`elf.rs:290-302`) limpia la caché tras copiar el segmento:

```rust
core::arch::asm!(
    "dc cvau, {dst}",
    ...
    dst = in(reg) paddr,
```

`dc cvau` opera sobre **una sola línea de caché**, la que contiene `paddr`. El
resto del segmento recién escrito queda solo en la caché de datos, mientras la
caché de instrucciones puede servir contenido rancio. Bajo QEMU con TCG no se
nota porque el emulador no modela cachés incoherentes; en hardware AArch64 real
el binario cargado falla de forma no determinista. Hay que iterar `dc cvau`
sobre todo el segmento en pasos del tamaño de línea (`CTR_EL0.DminLine`) antes
del `dsb ish` / `ic iallu`.

### Sitios afectados en `fs/tarfs.rs`

- `read_file` (`tarfs.rs:138`): `let end = start + entry.size;` con `size`
  procedente de la cabecera tar.
- `read_file` (`tarfs.rs:148`): `(entry.size + 511) / 512` desborda con un
  tamaño cercano al máximo.
- `parse_octal` (`tarfs.rs:269`): `val = val * 8 + ...` sin techo; una cadena
  octal larga desborda `usize` sin error.
- `normalize_path` (`tarfs.rs:214`) no colapsa `..`, de modo que una entrada
  llamada `../../x` se conserva tal cual en la tabla de rutas. Hoy solo se
  consulta por coincidencia exacta, pero deja la puerta abierta en cuanto
  aparezca una capa de extracción a disco.

## Alcance Técnico

1. Sustituir toda la aritmética de validación de `elf.rs` y `tarfs.rs` por
   `checked_add` / `checked_mul`, devolviendo el error correspondiente cuando
   no haya resultado representable.
2. Revalidar `file_off`, `file_sz` y `mem_sz` **dentro** de `load`,
   `load_segment` y `load_aarch64`, y no solo en `parse_elf`, para que las
   funciones `unsafe` no dependan de una validación remota.
3. Iterar el mantenimiento de caché de `load_aarch64` sobre la extensión
   completa del segmento, con el tamaño de línea leído de `CTR_EL0`.
4. Acotar `parse_octal` y hacer que `normalize_path` resuelva `.` y `..`,
   rechazando rutas que escapen de la raíz del archivo.
5. Evaluar activar `overflow-checks = true` en el perfil `release` del kernel:
   en bare metal un desbordamiento silencioso cuesta más que el tamaño del
   código añadido.

## Criterios de Aceptación

- Tests con imágenes ELF sintéticas donde `program_header_count`,
  `program_header_size`, `offset` y `file_size` toman valores que desbordan:
  `parse_elf` devuelve `Err` en todos ellos, compilado en `release`.
- Test de `parse_octal` con una cadena que desborda `usize`: devuelve error o
  un valor acotado, nunca un tamaño dado la vuelta.
- Test de `normalize_path` con `../../etc/passwd` y `a/./b/../c`.
- `load_aarch64` limpia la caché de todo el segmento; verificado por inspección
  del bucle y por un arranque en QEMU AArch64 que sigue cargando el binario de
  usuario.
- `cargo build --manifest-path kernel/Cargo.toml --target x86_64-unknown-none`
  y `--target aarch64-unknown-none` en verde, y
  `cargo clippy --manifest-path kernel/Cargo.toml -- -D warnings` limpio.

## Estado de Verificación (September 2026)

**Implementado** en [`kernel/src/elf.rs`](file:///Users/juandevelop/Develop/antOS/kernel/src/elf.rs),
[`kernel/src/fs/tarfs.rs`](file:///Users/juandevelop/Develop/antOS/kernel/src/fs/tarfs.rs)
y [`kernel/Cargo.toml`](file:///Users/juandevelop/Develop/antOS/kernel/Cargo.toml):

1. **`elf.rs`.** Dos ayudantes nuevos, `checked_bounds` y
   `checked_program_header_offset`, centralizan toda la aritmética de límites
   con `checked_add`/`checked_mul`. Se aplican en `parse_elf` (tabla de
   cabeceras, desplazamiento por entrada, `file_off`/`file_sz` del segmento) y
   **también dentro de `load` (x86_64), `load_segment` y `load_aarch64`**, que
   ya no confían en la validación remota de `parse_elf` — exactamente lo que
   pedía el punto 2 del Alcance. De paso se cerró un desbordamiento adyacente
   no listado explícitamente en el ticket pero del mismo origen: ni
   `load_segment` ni `load_aarch64` comprobaban `file_size <= memory_size`, así
   que un segmento con `file_size` mayor que `memory_size` copiaba más allá de
   la región ya puesta a cero y validada. Ambas funciones lo rechazan ahora.
   `load_segment` también rechaza `memory_size == 0` antes de calcular
   `(end - 1) / PAGE_SIZE`, que subdesbordaría con un segmento degenerado.
2. **Mantenimiento de caché en AArch64.** Nueva `dcache_line_size()` lee
   `CTR_EL0.DminLine` con `mrs`. `load_aarch64` itera `dc cvau` en pasos de
   `line_size` sobre la extensión completa de cada segmento (no solo la línea
   que contiene `paddr`), y ejecuta `dsb ish` / `ic iallu` / `dsb ish` / `isb`
   una sola vez al final del segmento, en vez de por línea.
3. **`tarfs.rs`.** `parse_octal` satura en vez de desbordar
   (`saturating_mul`/`saturating_add`). `normalize_path` se reescribió por
   completo: resuelve `.` y `..` con una pila de componentes, así que
   `../../etc/passwd` se colapsa a `/etc/passwd` y nunca escapa de la raíz.
   `read_file` (rama `Memory`) usa `checked_add` para `start + size`;
   `from_memory`, `from_block_device` y la rama `BlockDevice` de `read_file`
   sustituyeron `(size + 511) / 512` por `size.div_ceil(512)` con
   `checked_mul`/`checked_add` en la propagación del desplazamiento, y
   devuelven `FsError::IoError` en vez de desbordar en silencio.
4. **`overflow-checks` en `release`.** Se evaluó activarlo (punto 5) y se
   decidió mantenerlo activado en `kernel/Cargo.toml`: el perfil `dev` ya lo
   trae activado por defecto en Rust, y ese es precisamente el perfil con el
   que arranca hoy en CI y en los bancos de pruebas de periféricos (T28.10);
   que arranque ahí sin pánicos de desbordamiento es evidencia de que la
   aritmética no auditada del resto del kernel no lo dispara en un arranque
   legítimo, así que activarlo en `release` no añade riesgo nuevo sobre
   entradas legítimas y sí cierra la misma ventana de silencio para entradas
   manipuladas en el binario optimizado.

**Sobre la verificación de los tests (T31.16).** Se escribieron 5 tests nuevos
en `elf.rs` (imágenes ELF sintéticas con `program_header_offset`,
`file_size`/`memory_size` que desbordan, y un caso bien formado que debe
seguir aceptándose) y 5 en `tarfs.rs` (`parse_octal` saturado,
`normalize_path` con `../../etc/passwd` y `a/./b/../c`, y desbordamiento en
`read_file`), siguiendo el estilo `#[cfg(test)] mod tests` del resto del
árbol. **`cargo test` para el kernel sigue sin compilar** — confirmado de
nuevo en este mismo trabajo (`error[E0463]: can't find crate for «test»`),
el problema exacto que describe T31.16 y que la propia Fase 31 ya señalaba
como bloqueante para verificar este ticket. Ante esa limitación, se hizo una
verificación en tres capas independientes:

- **Réplica en el anfitrión.** La lógica de `checked_bounds`,
  `checked_program_header_offset`, `parse_elf`, `parse_octal` y
  `normalize_path` es `core`/`alloc` puro, sin dependencias del kernel, así
  que se copió literalmente a dos binarios independientes compilados con
  `rustc -O` (equivalente a `release`, para que un desbordamiento silencioso
  se manifestara igual que en el kernel) fuera del árbol del repositorio. Las
  cuatro imágenes ELF sintéticas con campos cerca de `u64::MAX` y el caso
  `file_size > memory_size` se rechazan; una imagen bien formada se sigue
  aceptando; `parse_octal` satura a `usize::MAX`; `normalize_path` resuelve
  correctamente los diez casos de `.`/`..` probados, incluida la travesía.
- **Verificación de tipos in situ.** Con `#[test]` sustituido temporalmente
  por `#[allow(dead_code)]` en una copia de trabajo (revertida después) y
  `cargo rustc --target x86_64-unknown-none -- --cfg test`, los cuerpos reales
  de los 10 tests nuevos —dentro del propio crate del kernel, con sus tipos
  reales— compilaron sin **ningún** error; el único fallo que queda
  (`#[panic_handler]` requerido, ausente bajo `cfg(test)`) es precisamente el
  vacío de arnés que T31.16 tiene que resolver, no un defecto de este código.
- **Arranque real en QEMU.** `./run.sh --test` (x86_64, perfil `dev`, que ya
  trae `overflow-checks` activado por defecto) pasa con el banner de arranque
  correcto, ejerciendo el mismo camino de carga de ELF/tarfs que arregla este
  ticket. `system/run-arm.sh --test` (AArch64) fallaba **igual antes de este
  ticket** (comprobado con `git stash` sobre el árbol limpio): es un problema
  preexistente de este entorno/versión de QEMU, no una regresión de T31.3.

**Verificado:**

- `cargo build --manifest-path kernel/Cargo.toml` para `x86_64-unknown-none` y
  `aarch64-unknown-none`, con y sin `--features limine`, en `dev` y en
  `release` (con `overflow-checks = true`): las ocho combinaciones compilan
  limpias.
- `cargo clippy --manifest-path kernel/Cargo.toml --target x86_64-unknown-none`
  → 41 avisos (antes: 44); `--target aarch64-unknown-none` → 44 avisos (antes:
  45). Ninguno nuevo; los que bajaron son `manual_div_ceil` resueltos por el
  cambio a `.div_ceil(512)`.
- `cargo test --workspace` (host) → 329/329, sin cambios: este ticket no toca
  `system/`.

