# T30.5 · Instalación de antOS Linux en Hardware Real y Dual-Boot

> **Estado:** 🔄 En Progreso
> **Fase:** Fase 30 (Escritorio antOS Linux — NixOS / Wayland, driver diario)
> **Fecha:** September 2026
> **Dependencias:** T30.2 (ISO gráfica), T30.3 (userland), T15.x / T24.4 (`antos install`, particionador, gestor de arranque UEFI).

## Descripción

`antos install` (T24.4) fue diseñado para desplegar el sistema base a disco.
Este ticket lo alinea con **antOS Linux** (NixOS + escritorio antOS), no con el
kernel bare-metal: instalar desde la ISO gráfica de T30.2 a un disco físico
NVMe/SATA, con el módulo de escritorio activado, y con dual-boot seguro
preservando otros SO.

## Alcance Técnico

1. **Flujo de instalación desde la ISO de antOS Linux:**
   - `antos install` detecta discos (T15.1), ofrece **Disco Completo**
     (confirmación textual `SI`) o **Dual-Boot** (preserva ESP y particiones
     ajenas).
   - Genera un `configuration.nix` / `hardware-configuration.nix` con
     `services.antos.desktop.enable = true` y el `autologinUser` elegido en el
     asistente (paso 3 de T24.4: hostname, usuario, timezone, keymap).
   - `nixos-install` (o el equivalente del instalador) sobre el objetivo
     montado en `/mnt`, copiando el *closure* del escritorio.

2. **Gestor de arranque UEFI y dual-boot (T15.3 / T24 gestor):**
   - Entrada NVRAM `antOS Linux` vía `systemd-boot`/`efibootmgr`.
   - Detección de Windows/otros Linux y entradas de arranque coexistentes.
   - Preservar la partición ESP existente en modo Dual-Boot.

3. **Post-instalación:**
   - Primer arranque entra directo al escritorio antOS (autologin Wayland).
   - `antos doctor` verifica el recinto (`ANTOS_WORKSPACE`/`ANTOS_STATE`) y que
     `antosd` y `antos-barra` levantan.
   - `nixos-rebuild switch` disponible para que el usuario evolucione el
     sistema declarativamente (la premisa de `system/nixos/`).

4. **Documentación (`docs/guia-live-usb-e-instalacion-fisica.md` y
   `docs/manual-de-comandos.md` Método 7):**
   - Paso a paso: grabar la ISO de antOS Linux, arrancar, `antos install`,
     Disco Completo vs Dual-Boot, reinicio al escritorio.
   - Dejar explícito que este flujo instala **antOS Linux** (Método 5/ISO
     gráfica), distinto del Live del kernel bare-metal.

## Criterios de Aceptación

- Desde la ISO gráfica de T30.2 arrancada en una VM con disco virtual,
  `antos install` en modo **Disco Completo** deja un sistema que **arranca solo
  al escritorio antOS** (autologin Wayland, barra anclada, `antosd` vivo).
- En modo **Dual-Boot** con un segundo SO presente, ambos siguen arrancables y
  la ESP ajena se conserva.
- El sistema instalado permite `nixos-rebuild switch` y `antos doctor` pasa.
- La entrada de arranque `antOS Linux` queda registrada en NVRAM UEFI.
- Documentado el flujo completo, diferenciado del Live bare-metal.
- `cargo test --workspace` (crates del instalador) en verde; `cargo clippy`
  sin regresión.

## Estado de Verificación (September 2026)

**Implementado:**

1. **`InstallConfig.keymap`** (`system/protocolo/src/system.rs`): campo nuevo
   con `#[serde(default = "default_keymap")]` (`"us"`) para
   retrocompatibilidad; `Default` actualizado. Hilado desde el asistente
   (`InstallTomlConfig.keymap` ya existía y ahora llega a `InstallConfig` vía
   `to_install_config` y las dos construcciones directas de `cli.rs`) y desde
   `exec/mod.rs` (`install.deploy`).
2. **`DeployEngine::generate_nixos_config`** (`system/antosd/src/installer/deploy.rs`,
   **nuevo**): escribe en `<target>/etc/nixos/`:
   - `flake.nix` — `inputs.antos.url = "path:/etc/nixos/antos"` (offline),
     `nixosConfigurations."<hostname>"` con `antos.nixosModules.{default,desktop}`
     + overlay `antosd`/`antos-barra` + `./configuration.nix`.
   - `configuration.nix` — `imports = [ ./hardware-configuration.nix ]`,
     `services.antos.enable`/`services.antos.desktop.enable = true`,
     `autologinUser`, `networking.hostName`, `time.timeZone`,
     `console.keyMap`, `users.users.<user>`, `networking.networkmanager`,
     `nix.settings.experimental-features`, `system.stateVersion`. Gestor de
     arranque: `systemd-boot` + `efi.canTouchEfiVariables`; en **dual-boot**
     añade `configurationLimit`/`timeout` y una nota de que la ESP es
     compartida y systemd-boot **encadena los demás SO sin tocar sus
     entradas**.
   - `hardware-configuration.nix` — relleno con cabecera «lo reemplaza
     `nixos-generate-config --root <target>`».
3. **`deploy_system`**: dos pasos nuevos — `nixos_configuration` (llama a
   `generate_nixos_config`) y `nixos_install`, que **solo describe**
   `nixos-generate-config --root` + `nixos-install --root … --flake …#<host>
   --no-root-passwd`. El `InstallReport` pasa de 5 a 7 pasos.

   > **Corrección (T36.1, 2026-09-21):** este punto decía «en real, lo
   > ejecuta». Era falso: no había ningún `Command::new("nixos-install")`
   > en `installer/`; el modo real escribía en un directorio sin montar,
   > con UUIDs fijos, y anunciaba el éxito. T36.1 hace que `--apply` aborte
   > con un error explícito, marca cada paso `executed = false` y el
   > informe `simulated = true`, corrige el `flake.nix` generado
   > (`paquete.nix` → `overlays.default`, módulo `llm`, `system`
   > detectado, nixpkgs fijado, copia del árbol) y lo evalúa en CI. La
   > instalación real es **T36.2**.
4. **Documentación**: `docs/guia-live-usb-e-instalacion-fisica.md` (callout de
   "dos ISOs" + **§4-bis** con la tabla del flujo NixOS) y
   `docs/manual-de-comandos.md` (Método 7: "dos destinos de `antos install`").

**Pruebas (macOS, este entorno):**

- `cargo test -p antosd deploy` → **6/6 OK**, incluidos los dos nuevos
  (`test_generate_nixos_config_clean_install` — asegura
  `services.antos.desktop.enable = true`, `autologinUser`, hostname, timezone,
  keymap, `systemd-boot`, `users.users."<user>"`, `stateVersion`, y el
  `flake.nix` con `nixosModules.desktop`; y
  `test_generate_nixos_config_dual_boot_preserves_esp` — la nota de ESP
  compartida y `configurationLimit`).
- `cargo test --workspace` → **318/318**, 0 fallos (los tests de `deploy_system`
  en `dry_run` actualizados a 7 pasos con `nixos_configuration`/`nixos_install`).
- `cargo check --workspace` limpio; `cargo clippy` sin avisos nuevos (97,
  todos preexistentes).

**Pendiente de verificación en hardware / VM con disco:**

- Ejecución real de `antos install` desde la ISO gráfica de T30.2 a un disco
  virtual: `nixos-install` completo y primer arranque al escritorio antOS
  (autologin, barra anclada).
- Modo Dual-Boot con un segundo SO presente: ambos arrancables, ESP ajena
  intacta, entrada `antOS Linux` en NVRAM.
- `nixos-rebuild switch --flake /etc/nixos#<hostname>` sobre el sistema
  instalado.

Queda en **🔄 En Progreso**: el generador de configuración NixOS está
**implementado, con tests en verde y evaluado en CI** (T36.1); la
instalación real a disco —particionado, `mkfs`, `nixos-install`— está
**implementada en T36.2** (tubería única real/simulada, secuencia de
comandos fijada por tests) junto con el smoke en QEMU
(`system/nixos/install-smoke.sh`), que necesita la ISO autosuficiente de
**T36.3** para ejecutarse. Este ticket se cierra con ese smoke en verde.
