# T36.2 · Instalación Real a Disco: Particionado, `nixos-install` y Verificación en QEMU

> **Estado:** 🔄 En Progreso
> **Fase:** Fase 36 (Instalable de verdad)
> **Prioridad:** 🔴 Alta
> **Fecha:** September 2026
> **Dependencias:** T36.1 (instalador honesto, `flake.nix` evaluable, ESP en
> `/boot`), T15.1 (`DiskManager`), T24.4 (asistente), T30.2 (ISO), T31.4
> (sin `sh -c`; argumentos como vectores).
> **Origen:** revisión del proceso de instalación (2026-09-21). Tras T36.1,
> `antos install --apply` aborta con «no implementado». Este ticket lo
> implementa: es el que convierte la ISO en un sistema instalado que
> arranca solo al escritorio, y el que cierra los «pendiente de verificación
> en hardware» de T30.5.

## Descripción

Un `antos install --apply` desde la ISO en vivo de antOS Linux tiene que
hacer, de verdad y en este orden: particionar, formatear, montar, generar
la configuración, instalar el *closure* del sistema, desmontar. Cada paso
con su comando real, su salida capturada y su error propagado. Sin
dependencias nuevas: `parted`, `mkfs.*`, `blkid`, `nixos-generate-config`
y `nixos-install` ya viajan en el perfil de instalador de NixOS que
importa [`iso.nix`](../../system/nixos/iso.nix); aquí se declaran
explícitamente para que no dependa de lo que nixpkgs decida mañana.

Se elige `parted` + `mkfs` invocados desde Rust frente a `disko`
(declarativo, pero una dependencia externa más y otra sintaxis que
mantener). Si en el futuro compensa, `disko` cabe detrás de la misma
`InstallConfig`.

## Alcance Técnico

1. **Particionado real** (`DiskManager::apply_partitioning`, `dry_run =
   false`):
   - **Disco completo:** `parted -s <dev> mklabel gpt mkpart ESP fat32 1MiB
     513MiB set 1 esp on mkpart antos-root ext4 513MiB 100%`. Sin swap en
     disco (el escritorio usa `zramSwap.enable = true` en el
     `configuration.nix` generado: un módulo de nixpkgs, nada nuevo).
   - **Dual-boot:** no se crea ESP: se reutiliza la existente (la primera
     partición con flag `esp`; si no hay, `bail!` con el motivo); la raíz
     va en el **mayor hueco libre** que devuelva `parted -s <dev> unit MiB
     print free`, y solo si mide ≥ 20 GiB. No se redimensiona nada (fuera
     de alcance): si no hay hueco, el mensaje dice cuánto falta y que hay
     que liberarlo desde el otro SO.
   - Nombre de partición: `<dev>p<n>` si el dispositivo termina en dígito
     (`nvme0n1`, `mmcblk0`), `<dev><n>` si no (`sda`). Hoy `deploy_system`
     asume `p2` siempre.
   - `partprobe <dev>` + `udevadm settle` antes de seguir.
2. **Formateo y UUIDs reales:** `mkfs.vfat -F32 -n ANTOS_ESP` solo sobre
   la ESP recién creada (nunca sobre una ESP ajena en dual-boot);
   `mkfs.ext4 -F -L antos-root` sobre la raíz; `blkid -s UUID -o value
   <part>` para rellenar `InstallReport` (adiós a `C12A-7328` y
   `3a8d8e62-…`).
3. **Montaje:** `mount <root> /mnt/target`, `mkdir -p /mnt/target/boot`,
   `mount <esp> /mnt/target/boot`. `deploy_system` comprueba en
   `/proc/mounts` que ambos están montados antes de escribir un solo
   fichero.
4. **Configuración:** `nixos-generate-config --root /mnt/target` (escribe
   el `hardware-configuration.nix` real) y después
   `generate_nixos_config` (T36.1) sobrescribe `configuration.nix` y
   `flake.nix`, copia `flake.lock` y el árbol de antOS desde la fuente que
   la ISO lleva en `/etc/antos/source` (T36.3) — o desde
   `ANTOS_SOURCE` si se define, para desarrollo.
5. **Instalación:** `nixos-install --root /mnt/target --flake
   /mnt/target/etc/nixos#<hostname> --no-root-passwd --no-channel-copy`,
   con `stdout`/`stderr` en *streaming* al asistente (la barra de progreso
   simulada de `cli.rs` se sustituye por las líneas reales del comando,
   filtradas). Un `nixos-install` que falla es un `bail!` con las últimas
   50 líneas, no un «✓».
6. **Contraseña del usuario:** el paso queda como *hook*
   (`DeployEngine::set_user_password`) que T36.4 alimenta desde el
   asistente; aquí, si `InstallConfig.password_hash` viene informado, se
   escribe con `nixos-enter --root /mnt/target -- chpasswd -e` pasando
   `user:hash` por `stdin` (nunca por argumento ni por `sh -c`).
7. **Cierre:** `sync`, `umount -R /mnt/target`, resumen con la entrada de
   arranque (`bootctl --esp-path=/mnt/target/boot list` antes de
   desmontar) y la pregunta de reinicio que el asistente ya tiene.
8. **Modo no interactivo:** `antos install --config install.toml --apply`
   hace todo lo anterior sin preguntas (el `SI` de disco completo se
   sustituye por `confirm_wipe = true` en el TOML). Es lo que usa la
   verificación automática.
9. **Verificación automatizada** (`system/nixos/install-smoke.sh`, nuevo):
   arranca en QEMU (OVMF, `-enable-kvm` si hay `/dev/kvm`) la ISO de T36.3
   con un `qcow2` vacío de 32 GiB y un `install.toml` inyectado por
   `-fw_cfg`/`9p`; el live ejecuta `antos install --config … --apply`;
   reinicia desde el disco (sin la ISO) y comprueba por consola serie /
   `ssh` que `greetd` abrió la sesión del usuario, que
   `/var/lib/antos/estado/antos.sock` responde a `QueryGitStatus` (mismo
   protocolo que `desktop_ipc.rs`) y que `antos-barra` está vivo. Segunda
   pasada **dual-boot**: disco pre-poblado con una ESP con
   `EFI/Microsoft/Boot/bootmgfw.efi` de relleno y una partición NTFS vacía;
   tras instalar, la ESP conserva ese fichero y `bootctl list` muestra la
   entrada de Windows además de la de antOS.
10. **CI:** job `antos-linux-install` (`ubuntu-latest`, KVM anidado
    disponible en los *runners* x86_64 de GitHub) que ejecuta el smoke tras
    `nix-iso` (T36.3). Si supera los 40 min, se deja en `agents-nightly.yml`
    y en `workflow_dispatch`, pero **no** se omite.

## Criterios de Aceptación

- Desde la ISO de antOS Linux arrancada en QEMU con disco vacío, `antos
  install --apply` en **Disco Completo** deja un sistema que, sin la ISO,
  **arranca solo al escritorio antOS** (autologin por `greetd`, barra
  anclada, `antosd` respondiendo por el socket).
- En **Dual-Boot** con una ESP ajena y otra partición presentes, ambas se
  conservan byte a byte (checksum antes/después de la ESP ajena), y
  `bootctl list` muestra la entrada de antOS y la del otro SO.
- El sistema instalado admite `sudo nixos-rebuild switch --flake
  /etc/nixos#<hostname>` **sin red** (todo está en el store copiado por
  `nixos-install`).
- Todos los `InstallStep` del informe llevan `executed = true` y los UUIDs
  son los de `blkid`.
- Cualquier fallo de `parted`/`mkfs`/`mount`/`nixos-install` aborta con el
  error real y deja el disco desmontado; ningún «✓» sin comando ejecutado.
- `install-smoke.sh` en verde en CI (o nightly con enlace desde `ci.yml`),
  para x86_64; aarch64 al menos con `workflow_dispatch`.
- T30.5 pasa a ✅ Completado con la evidencia del smoke.
- `cargo test --workspace` (tests de `installer/` en `dry_run` sin
  regresión) y `cargo clippy` en verde.

## Fuera de Alcance

- Redimensionar particiones existentes (NTFS/ext4) para hacer sitio.
- Cifrado LUKS de la raíz (T36.4 lo deja como opción; la ejecución
  `cryptsetup` es un ticket posterior).
- Elección de sistema de ficheros (btrfs/zfs) y esquemas con `/home`
  separado.
- Instalación en hardware físico concreto: se verifica en QEMU con OVMF;
  la prueba en un portátil real es manual y se anota en el ticket cuando
  se haga.

## Estado de Verificación (2026-09-21)

### 1. Los cambios

- **`system/antosd/src/installer/runner.rs`** (nuevo) — trait
  `InstallRunner` (`run`, `run_streaming`, `is_mountpoint`, `is_real`);
  `SystemRunner` (procesos reales, `stdout`/`stderr` en *streaming* por
  un canal con dos hilos lectores, últimas 50 líneas en el error, nunca
  `sh -c`); `SimulatedRunner` (graba cada comando literal, mantiene una
  tabla de particiones en memoria derivada del `DiskDevice` para que
  `print free` vea lo que `mkpart` creó, UUIDs sintéticos por `blkid`,
  `fail_on(<programa>)` para los tests de limpieza);
  `parse_parted_print_free` (formato `-m`, huecos `free`, flag `esp`).
- **`deploy.rs`** — **una sola tubería** `DeployEngine::install(config,
  workspace, runner, on_line)`: particionado (limpio: `parted … mklabel gpt
  mkpart ESP … set 1 esp on mkpart antos-root …`; dual: ESP existente
  reutilizada, raíz en el mayor hueco libre ≥ 20 GiB o error con cuánto
  falta; `partprobe` + `udevadm settle`; nombre `p<n>`/`<n>` según el
  dispositivo), formateo (`mkfs.vfat` solo sobre ESP nueva; `mkfs.ext4 -F
  -L antos-root`), `blkid` reales, montaje (raíz + `/boot`) comprobado con
  `is_mountpoint`, `nixos-generate-config` + `generate_nixos_config` (el
  `hardware-configuration.nix` real no se pisa), `nixos-install --flake …
  --override-input antos path:<destino>/etc/nixos/antos
  --no-write-lock-file` en *streaming*, sondeo de la ESP, `sync` +
  `umount -R`. Un fallo tras el montaje desmonta antes de devolver el
  error. `deploy_system` = `SimulatedRunner` en `dry_run`, `SystemRunner`
  si no. Precondiciones reales: Linux, root, 11 herramientas, fuente de
  antOS localizable, destino **sin** montar. `password_hash` →
  `initialHashedPassword` (validado) y `zramSwap.enable` en el
  `configuration.nix`; `nix.registry.{nixpkgs,antos}` en el flake generado
  para que ambas fuentes viajen en la closure y `nixos-rebuild` funcione
  sin red. Se retiraron del camino NixOS los pasos FHS heredados
  (`/etc/fstab`, `antosd.service` en `/usr/local/bin`): no tenían sentido
  sobre un destino NixOS.
- **`system/protocolo/src/system.rs`** — `InstallConfig.password_hash:
  Option<String>` (`skip_serializing_if`, no cambia el formato sin él).
- **`cli.rs`** — el asistente llama a `install` con `SimulatedRunner` /
  `SystemRunner` y vuelca la salida de `nixos-install` al `writer`;
  `InstallTomlConfig.{password_hash, confirm_wipe}`; `--config` +
  `--apply` con `clean_install` exige `confirm_wipe = true`; `--apply`
  manda sobre `dry_run` del fichero.
- **`antos ping`** (`ipc::ping`, `cmd_ping`) — conecta al socket, envía
  `QueryGitStatus` y sale 0/1: lo que el smoke ejecuta en el sistema
  instalado (y lo que `doctor --desktop` reutilizará en T36.5).
- **`system/nixos/module.nix`** — `services.antos.smoke.enable`: servicio
  `antos-smoke` que ejecuta como root el guion que QEMU pasa por
  `-fw_cfg name=opt/antos/smoke` (módulo `qemu_fw_cfg`); sin `fw_cfg` no
  hace nada. **`iso.nix`** lo activa.
- **`system/nixos/install-smoke.sh`** (host: QEMU/OVMF, KVM/HVF/TCG,
  x86_64 y aarch64, dos fases con veredicto por consola serie, sin red),
  **`install-smoke-guest-install.sh`** (live: en `dual` prepara ESP ajena
  con `bootmgfw.efi` de relleno + partición `other` y guarda su checksum;
  `antos install --config … --apply`; añade `smoke.nix` al `/etc/nixos`
  instalado y repite `nixos-install`; en `dual` compara el checksum de la
  ESP ajena y exige que `bootctl list` vea a Windows y a antOS) y
  **`install-smoke-guest-verify.sh`** (sistema instalado: `antos.sock`,
  `antos ping` como el usuario, sesión de `greetd`, `antos-barra`,
  `nixos-rebuild build` sin red).
- **`.github/workflows/install-smoke.yml`** — `workflow_dispatch`
  (`clean|dual|both`) + semanal: `nix build .#iso`, QEMU+OVMF, ambos
  modos, logs de la serie como artefacto. Enlazado desde `ci.yml`.
- **Docs** — manual Método 7, guía §4-bis (tabla con los comandos
  reales), este ticket.

### Desviaciones respecto al alcance, deliberadas

- **Contraseña:** `password_hash` va al `configuration.nix` como
  `users.users.<u>.initialHashedPassword` en vez de `nixos-enter … chpasswd
  -e`. Es declarativo (el mismo fichero que el usuario evoluciona después),
  no depende de un chroot sobre el destino, y `initialHashedPassword` deja
  que `passwd` la cambie luego. El *hook* para la contraseña en claro del
  asistente (T36.4) seguirá siendo `chpasswd` por `stdin`.
- **Partición ajena del smoke dual:** `ext4` con etiqueta `other` en vez de
  NTFS — `mkfs.ntfs` no está en el perfil de instalador y lo que se
  verifica (que la partición y la ESP ajena sobreviven byte a byte) no
  depende del sistema de ficheros.
- **Una tubería, dos runners** en vez de `apply_partitioning(dry_run)` +
  ramas: la simulación enseña los comandos literales de la instalación
  real y los tests fijan la secuencia entera.

### 2. Verificación ejecutada (macOS, este entorno)

- `cargo test --workspace` → **544 tests, 0 fallos**. Nuevos: 5 en
  `runner.rs` (parseo de `parted -m` con una tabla real de NVMe+Windows,
  `mkpart`/`mklabel`/`set` en la simulación, `SystemRunner` con `false`/
  `printf` y *streaming*), 11 en `deploy.rs` (secuencia exacta de 14
  comandos en dual-boot y de la instalación limpia, sin ESP → rechazo,
  sin hueco → «faltan N MiB», fallo de `nixos-install` → `umount -R`,
  fallo de `mkfs.ext4` → sin montar ni desmontar, `password_hash` válido /
  inválido / ausente, `hardware-configuration.nix` existente respetado,
  `partition_device`), `antos ping` con y sin demonio en
  `tests/desktop_ipc.rs`, `confirm_wipe` en el TOML.
- `cargo clippy` (ambas puertas) y `cargo fmt --check` limpios.
- Asistente interactivo en dry-run con el binario: seis pasos `○` con los
  comandos literales (`parted … mkpart antos-root ext4 461313MiB
  511999MiB`, `mkfs.ext4 -F -L antos-root /dev/disk0p3`, `mount …`,
  `nixos-install --root … --override-input antos path:…`, `umount -R …`).
- `bash -n` de los tres guiones; el conductor rechaza ISO/modo inválidos.
- `ruby -ryaml` valida `install-smoke.yml`.

### 3. Pendiente (por qué sigue 🔄 En Progreso)

Los criterios de aceptación que exigen **ejecutar** la instalación
(arranque al escritorio desde el disco, dual-boot con ESP ajena intacta,
`nixos-rebuild` sin red, T30.5 ✅) dependen de una ISO que lleve la
closure del sistema y el árbol fuente de antOS: hoy `nixos-install` dentro
del live tendría que compilar `antosd` y `antos-barra` sin red. Eso es
**T36.3**. El smoke (`install-smoke.sh`, workflow `install-smoke.yml`)
está escrito para ejecutarse tal cual en cuanto `nix build .#iso` produzca
esa ISO; el resultado se anota aquí y cierra T36.2 y T30.5. En esta máquina
(macOS, sin Nix ni `parted`) no es ejecutable.

### Criterios de aceptación — estado

- Disco Completo desde la ISO en QEMU arranca al escritorio: **⏳ smoke
  escrito; espera la ISO de T36.3** (la tubería y su secuencia de comandos
  están fijadas por tests).
- Dual-Boot conserva la ESP ajena y `bootctl list` ve ambos: **⏳ ídem**
  (la simulación fija que la ESP ajena nunca pasa por `mkfs.vfat`).
- `nixos-rebuild switch` sin red: **⏳ ídem** (`nix.registry` en el flake
  generado es lo que lo hace posible).
- `InstallStep.executed = true` y UUIDs de `blkid` en el informe real:
  **✅ en código** (`executed = runner.is_real()`, UUIDs de `blkid`).
- Fallos de `parted`/`mkfs`/`mount`/`nixos-install` abortan con el error
  real y desmontan: **✅** (tests `test_install_failure_*`).
- `install-smoke.sh` en CI: **⏳ workflow escrito** (`install-smoke.yml`);
  aarch64 por `ARCH=aarch64` en el mismo guion.
- T30.5 ✅: **⏳ con el smoke**.
- `cargo test --workspace` y `cargo clippy`: **✅**.
