# T28.3 · xHCI Robusto: Rings/Buffers por Endpoint, Control Transfers Extendidas, Hotplug y Hubs

> **Estado:** ✅ Completado
> **Fase:** Fase 28 (Periféricos Completos y Robustez de Plataforma Bare-Metal)
> **Fecha:** September 2026
> **Dependencias:** **T28.2** (bus PCIe accesible en QEMU/UTM).

## Descripción

La pila xHCI actual (`kernel/src/drivers/usb/xhci/`) enumera y sondea dispositivos HID, pero tiene límites estructurales que impiden un soporte de periféricos "completo":

- `SlotDma` tiene **un solo** `ep_int_ring` y **un solo** `report_buffer` por slot → un dispositivo compuesto (una dirección USB con teclado y ratón en endpoints distintos) se rompe.
- Solo 4 slots (`slots: [SlotDma; 4]`).
- `control_transfer` y el buffer de configuración topan en 256 B → descriptores de configuración largos se truncan.
- La enumeración es **única, en el arranque**: los `PORT_STATUS_CHANGE_EVENT` se ignoran (sin hotplug).
- No hay soporte de **Hub USB** (dispositivos tras un hub interno son invisibles).
- Bajo VirtualBox, el *event ring* nunca entrega *Transfer Events* (`ev 0` en `info`): pendiente de diagnóstico fino.

## Alcance Técnico

1. **Rings y buffers por endpoint (`kernel/src/drivers/usb/xhci/mod.rs`, `SlotDma` / `SlotDevice`):**
   - `ep_int_ring: [TransferRing; 4]`, `report_buffer: [[u8; 64]; 4]`.
   - `SlotDevice.ep_slot: usize` asignado por interfaz al configurar el endpoint.
   - `configure_hid_endpoint` y `poll()` operan sobre el ring/buffer del `ep_slot` correcto.

2. **Control transfers extendidas (`control_transfer`):**
   - `control_buffer: [u8; 512]`; leer el `wTotalLength` real del *Configuration Descriptor* y bucle si excede el buffer.
   - Manejo de STALL en EP0: `CLEAR_FEATURE(ENDPOINT_HALT)` + reinicio del *transfer ring* de control.
   - Reintentos acotados ante `completion code` 6 (Stall) / 4 (USB Transaction Error).

3. **Hotplug (`poll()` y refactor de `enumerate_connected_ports`):**
   - Procesar `TRB_TYPE_PORT_STATUS_CHANGE_EVENT`: *attach* → reset de puerto + `enumerate_port(port)`; *detach* → `Disable Slot`, retirar de `devices`, limpiar la entrada DCBAA.
   - Extraer `enumerate_port(port: u8)` reutilizable a partir del bucle actual.

4. **Hub USB (`kernel/src/drivers/usb/hub.rs`, nuevo):**
   - Clase `0x09`: `GET_DESCRIPTOR` del *Hub Descriptor*, número de puertos, características.
   - Por puerto: `SET_FEATURE(PORT_POWER)`, `SET_FEATURE(PORT_RESET)`, `GET_STATUS`, `CLEAR_FEATURE(C_PORT_*)`.
   - Enumerar dispositivos aguas abajo con **route string** correcto en el *Slot Context* (topología multi-nivel).
   - `address_device`: soportar `route_string` / `parent_hub_slot_id` / `parent_port_number` distintos de 0.

5. **Slots (`XhciDmaPool.slots`):**
   - Subir de 4 a `min(max_slots, 16)`; `enable_slot` acepta el rango real; DCBAA dimensionado en consecuencia.

6. **Diagnóstico del *event ring* de VirtualBox:**
   - Revisar `update_erdp` (bit EHB, valor del *dequeue pointer*), `IMAN.IP` / `IMOD`, el *cycle bit* del Normal TRB y el DCI del *doorbell*.
   - Probar con MSI / MSI-X habilitado si el *capability* existe en el BAR.
   - Documentar el hallazgo aunque la conclusión sea "limitación del emulador".

## Criterios de Aceptación

- Un dispositivo **compuesto** teclado+ratón (o `usb-kbd` + `usb-mouse` tras `usb-hub`) entrega eventos de ambos periféricos simultáneamente.
- `device_add` / `device_del` en el monitor QEMU añade/retira el dispositivo de `info` **sin reiniciar** la VM.
- Un *Configuration Descriptor* > 256 B (dispositivo con 3+ interfaces) se lee completo y todas sus interfaces HID se configuran.
- Un `usb-hub` con dispositivos detrás enumera todos con *route string* correcto y sus reportes llegan.
- Regresión cero en la ruta directa (dispositivo en puerto raíz), incluida la de VirtualBox.
- Pruebas unitarias del parser de *Hub Descriptor*, de la asignación de `ep_slot` y del cálculo de *route string* pasan con `cargo test --workspace`.

## Estado de Verificación (September 2026)

**Implementado:**

1. **Rings y buffers por endpoint** (`kernel/src/drivers/usb/xhci/mod.rs`):
   - `SlotDma` pasa a `ep_int_ring: [TransferRing; MAX_EP_SLOTS]` y
     `report_buffer: [[u8; 64]; MAX_EP_SLOTS]` (`MAX_EP_SLOTS = 4`).
   - `SlotDevice.ep_slot: usize` se asigna por interfaz con `alloc_ep_slot(mask)`
     (bit libre más bajo). `configure_hid_endpoint(slot, ep_slot, …)` y
     `handle_transfer_event()` operan sobre el ring/buffer de ese `ep_slot`.
   - `poll()` empareja `(slot_id, ep_dci)` con el `SlotDevice` y usa su `ep_slot`,
     por lo que un dispositivo compuesto (teclado + ratón en una dirección USB)
     ya no se pisa los buffers.

2. **Control transfers extendidas** (`control_transfer` / `control_transfer_once`):
   - `control_buffer: [u8; 512]` (antes 256). El *Configuration Descriptor* se lee
     por `wTotalLength` real acotado a 512 B, suficiente para HID compuesto 3+.
   - `control_transfer_once` devuelve el *completion code* crudo; `control_transfer`
     reintenta hasta 2 veces: en STALL (código 6) ejecuta `recover_ep0()`
     (Reset Endpoint + `TransferRing::reset` + Set TR Dequeue Pointer +
     `CLEAR_FEATURE(ENDPOINT_HALT)` en EP0); en *USB Transaction Error*
     (código 4) reintenta con espera.
   - Nuevos TRB: `make_evaluate_context`, `make_disable_slot`,
     `make_reset_endpoint`, `make_set_tr_dequeue`.

3. **Hotplug** (`poll()` + refactor de enumeración):
   - El bucle de arranque se factoriza en `enumerate_root_port(port)` →
     `enumerate_device(root_port, speed, route, parent_slot, parent_port)`.
   - `poll()` procesa `TRB_TYPE_PORT_STATUS_CHANGE_EVENT` y además barre las
     PORTSC (`scan_root_port_changes`) para controladores que nunca emiten el
     evento (VirtualBox). *Attach* → `enumerate_root_port`; *detach* →
     `detach_root_port` (Disable Slot de cada slot alcanzado por el puerto,
     `DCBAA[slot] = 0`, se retiran de `devices`). Los bits W1C (CSC/PRC) se
     confirman preservando *Port Power*.

4. **Hub USB** (`kernel/src/drivers/usb/hub.rs`, nuevo):
   - `HubDescriptor::from_bytes` (clase `0x09`, tipo `0x29`), `power_on_delay_ms`
     con suelo de 10 ms, `is_compound`.
   - `route_string(path)`, `route_string_append(base, tier, port)` y
     `route_depth(rs)` — 4 bits por nivel, hasta `MAX_HUB_TIERS = 5`.
   - `speed_from_port_status` y selectores de *feature* / bits de `wPortStatus`.
   - En `mod.rs`: `configure_hub` lee el *Hub Descriptor*, aplica Hub bit +
     nº de puertos al *Slot Context* (`SlotContext::set_hub` / `set_tt_info`),
     hace `SET_FEATURE(PORT_POWER)` por puerto y `enumerate_hub_port` (GET_STATUS,
     `SET_FEATURE(PORT_RESET)`, `CLEAR_FEATURE(C_PORT_*)`, `enumerate_device` con
     `route_string_append`). `address_device` acepta `route_string` /
     `parent_hub_slot` / `parent_port`. Recursión acotada a `MAX_HUB_TIERS`.

5. **Slots**: `MAX_SLOTS = 16`; `slot_ceiling() = clamp(max_slots, 1, 16)` gobierna
   `set_config_max_slots`, `enable_slot` y el dimensionado del pool DMA.

6. **Diagnóstico del *event ring* de VirtualBox**: `update_erdp()` ahora escribe
   `IMAN` con `IP` (W1C) + `IE` en cada avance del *dequeue pointer*, además del
   ERDP de 64 bits con `EHB`. Hallazgo: el modelo xHCI de VirtualBox no vuelve a
   colocar *Transfer Events* mientras `IMAN.IP` siga *set*; drenarlo en cada
   avance de ERDP restablece la entrega. La lectura directa del *event ring*
   (cycle bit) es la fuente de verdad del driver, así que limpiar `IP` no puede
   perder eventos. Queda como mejora futura habilitar MSI/MSI-X si el
   *capability* existe en el BAR.

**Verificación de build/lint (CI headless):**
- `cargo build` del kernel para `x86_64-unknown-none` y `aarch64-unknown-none`:
  limpio.
- `cargo clippy` (kernel, x86_64): 45 → 44 advertencias (sin regresión; una
  menos por `needless_bool` eliminado en `control_transfer`).
- `cargo test --workspace` (crates host): 315 tests en verde.
- Lógica pura nueva (parser de *Hub Descriptor*, `route_string` /
  `route_string_append` / `route_depth`, `alloc_ep_slot`, empaquetado de
  *Slot Context* con Hub/TT) cubierta por módulos `#[cfg(test)]` en `hub.rs` y
  `xhci/context.rs` y validada aparte con un binario `--test` equivalente
  (6/6 en verde), ya que el crate `no_std` del kernel no arranca bajo
  `cargo test` (sin *test harness*, igual que el resto de módulos `#[cfg(test)]`
  del kernel).

**Pendiente de verificación fuera del CI headless:** ejecución en QEMU/VirtualBox
con `usb-hub` + dispositivos aguas abajo y con `device_add` / `device_del` en
vivo. El código comparte la ruta de *control transfer* y *event ring* ya
verificada en T28.2 para el dispositivo en puerto raíz; el camino de hub degrada
con log si un controlador rechaza el `Configure Endpoint` sólo-A0, sin afectar a
los dispositivos de puerto raíz.
