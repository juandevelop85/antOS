# T12.2 · Edición Colaborativa en Vivo Humano-Agente y Protocolo DAP de Depuración Aislada

> **Estado:** ✅ Completado  
> **Fase:** Fase 12  
> **Fecha:** September 2026

## Descripción
Protocolo de edición concurrente en tiempo real mediante cursores virtuales y deltas CRDT entre el desarrollador y el agente `Coder`, permitiendo programar en pareja humano-IA sin conflictos de edición, con soporte complementario para el protocolo de depuración aislada DAP (`Debug Adapter Protocol`).

## Alcance Técnico
1. **Motor de Co-Edición CRDT (`system/antosd/src/collab/`):**
   - Manejo de deltas de edición basados en CRDTs de texto livianos (estilo Automerge o Yjs simplificado en Rust).
   - Proyección de la posición del cursor del agente y sugerencias de código fantasma (*ghost text*) en el editor del usuario.
2. **Adaptador DAP Aislado:**
   - Servidor DAP para inspeccionar variables, pausar ejecución y evaluar expresiones dentro del sandbox confinado sin otorgar permisos adicionales.
3. **Capacidades Declarativas:**
   - `collab.session`: Inicia o une una sesión de co-programación con un rol de agente.
   - `dap.attach`: Conecta una sesión de depuración a un proceso sandbox supervisado.
4. **Comandos CLI e Integración en antOS:**
   - `antos pair [ticket_id]`: Inicia sesión interactiva de pair programming con el Coder.
   - `antos debug <comando>`: Lanza el comando en sandbox bajo supervisión DAP.
5. **Pruebas y Verificación:**
   - Pruebas unitarias de convergencia de deltas concurrentes en el mismo buffer de texto.
   - Validación del protocolo JSON-RPC de DAP para comandos básicos de inspección.

## Criterios de Aceptación
* El desarrollador y el agente pueden editar el mismo archivo simultáneamente convergiendo a un estado consistente.
* `cargo test --workspace` pasa al 100% sin advertencias ni regresiones.
* Nomenclatura e identificadores en Rust 100% en inglés.

## Corrección del Estado Real del Adaptador DAP (September 2026 — T31.14)

La auditoría de código de Fase 31 encontró que el Alcance Técnico punto 2
(«Adaptador DAP Aislado... dentro del sandbox confinado») y el punto 4
(«`antos debug <comando>`: Lanza el comando en sandbox bajo supervisión
DAP») **no se cumplen**. La parte CRDT del ticket sí es real y su criterio
de aceptación se sostiene; la parte DAP es una maqueta completa:

- `DapServer::new` no lanza el `command` que recibe, ni ningún depurador
  real (`gdb`, `lldb`, o cualquier cliente del Debug Adapter Protocol). El
  comando se guarda como texto pero nunca se ejecuta — mucho menos dentro
  de un sandbox, porque no hay ningún proceso al que confinar.
- La pila de llamadas y las variables que devuelve son una lista fija de
  ejemplo (`"main() at src/main.rs:1"`, una variable `is_sandboxed: "true"`
  literal…), idéntica sin importar qué comando se le pase.
- `add_breakpoint` marca `verified: true` siempre, porque no hay ningún
  depurador real que pudiera rechazar la línea.
- `antos debug <comando>` imprimía «Comando en sandbox: …», dando a
  entender ejecución real y confinada de un comando que en realidad nunca
  se ejecuta.

**Corregido por
[T31.14](file:///Users/juandevelop/Develop/antOS/docs/tickets/T31.14-alineacion-de-la-documentacion-de-modulos-con-la-implementacion-real.md)**
(sin reabrir este ticket, documentado aquí para que el estado real quede
escrito donde se originó la promesa):

- Nueva cabecera de módulo en `crate::collab` que distingue explícitamente
  el CRDT real (`CrdtDocument`, un RGA propio con desempate determinista)
  del `DapServer` simulado.
- Nuevo `antos_protocol::DapSessionStatus::simulated: bool` (siempre
  `true` hoy), para que ningún consumidor por IPC pueda confundir esta
  maqueta con una sesión de depuración real.
- `antos debug` y el formateador de `Change::DapAttach` en `exec/mod.rs`
  ya no dicen «Comando en sandbox»: indican explícitamente que la sesión
  es simulada y que el comando no se ejecuta.
- Test `test_dap_status_is_honestly_marked_as_simulated` fija
  `simulated == true` con un comando deliberadamente peligroso
  (`"rm -rf /"`) como demostración de que en efecto nunca se ejecuta nada.

**Sigue sin existir** tras T31.14: cualquier cliente real del Debug
Adapter Protocol, cualquier depurador lanzado, y por tanto cualquier
aislamiento de sandbox sobre él — no hay nada que aislar. Implementar un
adaptador DAP real (lanzar el depurador, hablar el protocolo, adjuntarlo al
proceso vía `sandbox::run`) queda fuera del alcance de una corrección de
auditoría: es un ticket propio de infraestructura de depuración.
