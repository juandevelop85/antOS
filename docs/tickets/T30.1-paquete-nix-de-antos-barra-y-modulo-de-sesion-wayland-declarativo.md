# T30.1 · Paquete Nix de `antos-barra` y Módulo de Sesión Wayland Declarativo

> **Estado:** 🔄 En Progreso
> **Fase:** Fase 30 (Escritorio antOS Linux — NixOS / Wayland, driver diario)
> **Fecha:** September 2026
> **Dependencias:** T13.0 (sesión Labwc y `system/desktop/`), T25.4 (lanzador de la barra), `system/nixos/` (módulo `services.antos`).

## Descripción

`system/nixos/` monta hoy sólo el demonio `antosd` (VM *headless*, `htop` y
poco más). La barra de escritorio (`system/barra`, GTK4 + `wlr-layer-shell`,
1352 líneas) **no tiene paquete Nix** (está excluida del workspace) y la
sesión Wayland de `system/desktop/` es un script suelto (`start-session.sh`
copia `rc.xml`/`autostart` a `~/.config/labwc`), no algo declarativo.

Este ticket empaqueta la barra con Nix y añade un módulo
`services.antos.desktop` que describe la sesión completa —compositor Labwc +
`antos-barra` + `antosd` + variables XDG + autologin a Wayland— de forma
declarativa, para que activarlo sea una línea del `configuration.nix`.

## Alcance Técnico

1. **Paquete Nix de `antos-barra` (`system/nixos/barra.nix`):**
   - `rustPlatform.buildRustPackage` sobre `system/barra` (workspace propio),
     con `nativeBuildInputs`/`buildInputs` de GTK4, `gtk4-layer-shell`, wayland,
     glib, pango, cairo, gdk-pixbuf.
   - Instala el binario `antos-barra` y el CSS (`system/barra/src/estilo.css`)
     en `$out/share/antos-barra/`.
   - Referenciado desde `overlay`/`paquete.nix` como `pkgs.antos-barra`.

2. **Módulo `services.antos.desktop` (`system/nixos/desktop.nix`):**
   - Opción `enable`, `compositor` (por defecto `pkgs.labwc`),
     `autologinUser`, `terminal` (por defecto `foot`), `editor`
     (por defecto `neovim`).
   - Cuando `enable`:
     - `environment.systemPackages`: compositor, `antos-barra`, `antosd`,
       terminal, `wl-clipboard`, `grim`/`slurp` (para `wlr-screencopy`),
       `xdg-desktop-portal-wlr`.
     - Genera `/etc/antos/desktop/rc.xml` y `/etc/antos/desktop/autostart` a
       partir de `system/desktop/` (los mismos atajos: `Super+Space`,
       `Super+A`, `Super+Return`, `Super+W`), y un fichero de sesión Wayland
       `antos.desktop` en `${pkgs.dbus}/share/wayland-sessions` equivalente.
     - `environment.sessionVariables` con el contenido de
       `system/desktop/environment` (`XDG_CURRENT_DESKTOP=antOS`,
       `GDK_BACKEND=wayland`, `EDITOR=nvim`, `ANTOS_COMPOSITOR=labwc`, …).
     - `services.greetd` (o autologin de `getty` + `exec labwc`) que arranca
       la sesión antOS para `autologinUser`.
     - `services.antos.enable = true` implícito (levanta `antosd`).
   - `autostart` lanza `antosd` (si no corre) y `antos-barra` como cliente
     layer-shell, igual que `system/desktop/autostart` pero con rutas del
     store.

3. **Integración con `system/nixos/configuracion.nix`:** añadir
   `services.antos.desktop.enable = true;` como opción comentada + documentada;
   `vm.nix` se resuelve en T30.2.

4. **Retrocompatibilidad:** `system/desktop/start-session.sh` sigue existiendo
   para lanzar la sesión sobre un Linux no-NixOS (dev), reusando los mismos
   `rc.xml`/`autostart`/`environment` que consume el módulo.

## Criterios de Aceptación

- `nix build` del paquete `antos-barra` produce un binario ejecutable con sus
  dependencias GTK4/Wayland resueltas.
- Un `configuration.nix` con `services.antos.desktop.enable = true` evalúa sin
  errores (`nixos-rebuild build`) e incluye el compositor, `antos-barra`,
  `antosd`, la terminal y el editor en el sistema.
- Los ficheros `rc.xml` / `autostart` / variables de entorno generados en
  `/etc/antos/desktop` coinciden en atajos y semántica con `system/desktop/`.
- `system/desktop/start-session.sh` sigue funcionando sobre un Linux con
  `labwc` en el PATH (sin regresión de T13.0).
- Documentado en `docs/manual-de-comandos.md` (Método 5) y en
  `docs/arquitectura.md`.
- `cargo build -p antos-barra` (en Linux) y `cargo check --workspace`
  (crates host) sin regresión.

## Estado de Verificación (September 2026)

**Implementado:**

1. **`system/nixos/barra.nix`** — `rustPlatform.buildRustPackage` de
   `antos-barra`: `src` acotado a `system/barra` + `system/protocolo`
   (dependencia por ruta), `sourceRoot = "source/system/barra"` (workspace
   propio), `cargoLock.lockFile = system/barra/Cargo.lock`,
   `nativeBuildInputs = [pkg-config wrapGAppsHook4]`,
   `buildInputs = [gtk4 gtk4-layer-shell wayland glib]`,
   `meta.platforms = linux`. El CSS ya viaja embebido (`include_str!`), así que
   no hay recursos que instalar aparte del binario.
2. **`system/nixos/desktop.nix`** — módulo `services.antos.desktop` con
   opciones `enable`, `compositor` (`pkgs.labwc`), `autologinUser` (`antos`),
   `terminal` (`pkgs.foot`), `editor` (`pkgs.neovim`), `barra`
   (`pkgs.antos-barra`). Al activarse:
   `services.antos.enable = mkDefault true`; `environment.systemPackages` con
   compositor + barra + terminal + editor + `wl-clipboard`/`grim`/`slurp`/`git`;
   `environment.sessionVariables` con el contenido de
   `system/desktop/environment`; `environment.etc."antos/desktop/{rc.xml,
   autostart,environment}"` desde `system/desktop/`; `services.greetd` con
   `default_session` apuntando a un guion generado (`writeShellScript`) que
   instala la config de Labwc en `~/.config/labwc` y ejecuta el compositor;
   `users.users.<autologinUser>`, `hardware.graphics.enable`,
   `programs.dconf.enable`, `xdg.portal` (`xdg-desktop-portal-wlr`), fuentes.
3. **`flake.nix`** — overlay `overlayAntos` (añade `antos-barra`),
   `packages.<sys>.antos-barra`, `nixosModules.desktop`, `desktop.nix` incluido
   en `base` (la opción existe siempre, deshabilitada), y
   `nixosConfigurations.antos-desktop` (máquina con
   `services.antos.desktop.enable = true`).
4. **`system/nixos/configuracion.nix`** — opción comentada + documentada.
5. **`system/desktop/start-session.sh`** — nota de retrocompatibilidad; el
   guion sigue lanzando la sesión sobre un Linux no-NixOS sin cambios.
6. **Documentación** — `docs/manual-de-comandos.md` Método 5 (sección
   "Escritorio antOS Linux") y `docs/arquitectura.md` §5.

**Pruebas (contenedor `nixos/nix` sobre `podman`, aarch64-linux):**

- `nix flake metadata` → OK (resuelve el flake y el input `nixpkgs` pinado).
- `nix eval .#nixosConfigurations.antos.config.system.build.toplevel.drvPath`
  → `nixos-system-antos-…drv` (la máquina base evalúa con `desktop.nix`
  presente pero deshabilitado).
- `nix eval .#nixosConfigurations.antos-desktop.config.system.build.toplevel.drvPath`
  → `nixos-system-antos-…drv` (**el bloque `config` de `desktop.nix` con
  `enable = true` evalúa sin errores**: greetd, systemPackages, sessionVariables,
  xdg.portal, hardware.graphics, users).
- `nix eval .#packages.aarch64-linux.antos-barra.drvPath`
  → `antos-barra-0.1.0.drv` (derivación bien formada).
- `nix eval … environment.systemPackages` → contiene `antos-barra` (`true`).
- `nix eval … services.greetd.settings.default_session.command`
  → `…-antos-desktop-session` (autologin cableado).
- `nix build --dry-run .#antos-barra` → grafo de dependencias completo
  resuelto (gtk4 y su clausura llegan prebuilt de `cache.nixos.org`).
- `cargo check --workspace` y `cargo test --workspace` (crates host,
  315/315) → sin regresión (T30.1 no toca código Rust).

**Pendiente de verificación en un Linux con más RAM (o al completar el build
del contenedor):**

- `nix build .#antos-barra` **completo** (compilación real del crate contra
  GTK4). No verificable de forma fiable en la `podman-machine` actual (2 GiB;
  el propio `system/barra/Cargo.toml` documenta que rustc murió por OOM ahí).
- `cargo build -p antos-barra` nativo en Linux con GTK4.
- Arranque real de la sesión (Labwc + `antos-barra` anclada) — corresponde a
  **T30.4** (verificación end-to-end); requiere además la imagen gráfica de
  **T30.2**.

Por eso el ticket queda en **🔄 En Progreso**: el diseño Nix está escrito y su
**evaluación** verificada; falta la construcción/ejecución real sobre Linux.
