# T34.4 · Perfiles `local` / `hybrid` / `cloud` y Fiabilidad de los Roles con Modelos Pequeños

> **Estado:** ✅ Completado
> **Fase:** Fase 34 (Modelos locales de serie)
> **Prioridad:** 🟡 Media
> **Fecha:** September 2026
> **Dependencias:** T34.2 (contexto, `doctor`, `auto` local), T19.4 (modelos
> por rol), T33.3 (roles sobre el runtime), T33.5 (evaluación y `eval diff`).
> **Origen:** Los valores por defecto de `LlmConfig::get_role_model`
> contradicen el objetivo de «todo con modelos gratuitos»: Arquitecto →
> `openrouter:deepseek/deepseek-r1:free`, QA y Auditor →
> `groq:llama-3.3-70b-versatile`; solo el Coder es local. Una instalación
> sin claves no puede correr el pipeline de roles aunque tenga Ollama sano.
> Y la primera sesión con el 7B (T33.5 §3) dejó la fiabilidad del escenario
> Rust en 2/5 con n=1 por caso: los prompts y toolsets de los roles se
> escribieron pensando en modelos grandes, y no hay una medida repetida
> que diga si un cambio los mejora.

## Descripción

Dos mitades que se apoyan mutuamente: **perfiles** que fijan de un golpe
qué modelo usa cada rol según lo que la máquina tiene, y **medida
repetida** que convierte «funciona a veces» en un número que se puede
subir cambiando prompts, `format` y toolsets.

### 1. Perfiles

`antos llm profile <local|hybrid|cloud>` escribe `role_models` y
`role_steps` en `LlmConfig`:

| Perfil | Arquitecto | Coder | QA | Auditor | Requiere |
|---|---|---|---|---|---|
| `local` | Ollama, modelo «grande» de la tabla de RAM | Ollama, modelo «código» | Ollama, modelo «rápido» | Ollama, modelo «grande» | solo Ollama sano |
| `hybrid` | nube gratuita con clave presente (OpenRouter / Groq / Gemini) | Ollama | Ollama | nube gratuita | Ollama + ≥ 1 clave |
| `cloud` | como hoy (`get_role_model` actual) | como hoy | como hoy | como hoy | claves |

Los nombres «grande / código / rápido» se resuelven contra la tabla de RAM
de T34.2 (p. ej. 18 GB → `qwen2.5-coder:14b` no cabe con 16k de contexto;
el perfil elige `7b` para los tres y lo dice). `antos llm profile` sin
argumento muestra el activo y qué modelo tocó cada rol.

**El perfil por defecto de una instalación limpia es `local`**: si
`role_models` está vacío y `active_provider` es `auto`, `get_role_model`
devuelve el modelo local de `doctor`, no una URL de OpenRouter. El
comportamiento actual queda íntegro bajo `cloud`. `hybrid` se aplica solo
si hay clave; si no, avisa y aplica `local`.

### 2. Fiabilidad con modelos pequeños

Cambios en el runtime y los roles, cada uno medido antes y después:

- **Salida estructurada forzada.** El plan del Arquitecto y el veredicto
  del Auditor son JSON tipado; con Ollama se pide con `format: <JSON
  Schema>` (Ollama ≥ 0.5) en vez de confiar en que el modelo lo escriba
  bien. Igual en OpenAI-compat donde haya `response_format`. Claude sigue
  con `tool_use`, que ya lo garantiza.
- **Toolsets por tamaño.** Un 7B con doce herramientas confunde `fs.write`
  con `fs.patch`; `Role.toolset` gana una variante reducida (`compact`)
  que se activa cuando el modelo del rol es local y pequeño (umbral en la
  tabla de modelos): Coder = `fs.read`, `fs.patch`, `test.run`,
  `finalizar`; QA = `test.run`, `finalizar`.
- **Argumentos malformados → un reintento dirigido.** Cuando
  `Catalog::validate` rechaza una llamada por tipo/campo (no por política),
  el runtime devuelve el error **junto al esquema del parámetro** y
  permite un reintento sin consumir paso de presupuesto (una vez por
  llamada). Los rechazos por política siguen contando.
- **Prompts por rol en `agent/prompts/*.txt`** revisados con un modelo de
  7B: frases cortas, un ejemplo de llamada, «llama a `finalizar` en cuanto
  `test.run` esté en verde» al principio y al final. Cada cambio de prompt
  va con su medida en el ticket.

### 3. Medida

- `antos eval agent --live --provider ollama --repeat N` ejecuta cada caso
  N veces y guarda **tasa de éxito, mediana de pasos y de segundos**, no
  un único run. `eval diff` compara tasas y solo señala regresión si la
  caída supera un margen configurable (5 repeticiones tienen ruido).
- Job **nocturno** en GitHub Actions (`schedule:`), separado de `ci.yml`,
  que arranca Ollama por `nix run`, descarga `qwen2.5-coder:7b` (caché de
  Actions para `OLLAMA_MODELS`), ejecuta `--live --repeat 5` sobre los
  casos `live = true` y sube el JSON como artefacto. No bloquea PRs: es
  la serie histórica contra la que se mira antes de tocar un prompt.
- Los resultados con n=5 de este ticket se anotan en el propio ticket
  (antes / después de cada cambio), como hizo T33.5 §3.

## Alcance Técnico

- **`llm/config.rs`:** `Profile { Local, Hybrid, Cloud }`, `apply_profile`,
  `get_role_model` con el nuevo valor por defecto y la resolución por
  tabla; tests de los tres perfiles con y sin claves.
- **`agent/providers.rs`:** `format` en Ollama y `response_format` en
  OpenAI-compat cuando el `ToolSpec` terminal del rol declara esquema de
  salida; el reintento por validación en `agent/mod.rs`.
- **`agent/roles.rs`:** `toolset_compact`, criterio de activación por
  modelo; `agent/prompts/*.txt` revisados.
- **`agent/eval.rs`:** `--repeat`, agregados por caso, `diff` sobre tasas.
- **`.github/workflows/agents-nightly.yml`** (nuevo).
- **Manual §4.2:** perfiles, qué esperar de cada tamaño de modelo, y el
  ritual «evaluar → cambiar → evaluar → diff» con `--repeat`.

## Criterios de Aceptación

- `antos llm profile local` en una máquina con solo Ollama deja los
  cuatro roles en modelos locales que caben en RAM con 16k de contexto;
  `antos agent run <ticket> --auto` llega a `ReadyForApproval` sin clave.
- Test: `LlmConfig` limpia + `auto` → `get_role_model("architect")` es
  local; bajo `cloud` devuelve el valor de hoy.
- Test: un plan del Arquitecto devuelto como texto libre por un Ollama
  simulado **con** `format` no ocurre (el cuerpo enviado lleva el esquema);
  el runtime rechaza y reintenta una llamada con argumento malformado sin
  consumir paso, y no reintenta un rechazo por política.
- Test: el Coder con modelo pequeño recibe `toolset_compact`; con Claude,
  el completo.
- `antos eval agent --live --provider ollama --repeat 5` produce tasas por
  caso; `eval diff` con dos JSON de ejemplo (tasa 5/5 → 2/5) señala
  regresión y (5/5 → 4/5) no.
- Fiabilidad del escenario Rust con `qwen2.5-coder:7b`, n=5, anotada en el
  ticket antes y después; objetivo ≥ 4/5 en el Mac de 18 GB. Si no se
  alcanza, el ticket documenta qué cambio aportó qué y qué queda.
- Job nocturno en verde al menos una vez con el artefacto subido.
- `cargo test --workspace`, clippy `-D warnings`, `fmt`; CI verde.

## Fuera de Alcance

- Modelos de embeddings locales para `memory.search` (hoy `TermDictionary`
  léxico, T32.7): ticket aparte si se quiere.
- Cuantizaciones personalizadas o `Modelfile`s: el usuario que los quiera
  usa `ollama create` y los referencia por nombre.

## Estado de Verificación

### 1. Los cambios

- **Perfiles (`llm/config.rs`):** `Profile { Local, Hybrid, Cloud }`,
  `ProfileInputs`, `apply_profile` (escribe `role_models` y `profile`),
  `cloud_role_model` (el reparto de T19.4, ahora solo bajo `cloud`).
  **`get_role_model` cambia de valor por defecto:** sin `role_models`, con
  `active_provider` `auto`/`ollama`/`local` devuelve `ollama` a secas para
  todos los roles; con un proveedor de nube activo, ese. `providers::
  resolve` con `ollama` sin modelo usa el configurado si está descargado y,
  si no, el candidato local con `tools` (el recomendado por RAM o el
  primero capaz). CLI `antos llm profile [local|hybrid|cloud]`
  (`tools/llm/profile.rs`): tier de RAM de `models.toml`, clave de nube
  detectada (OpenRouter → Groq → Gemini → Claude); `hybrid` sin clave avisa
  y aplica `local`.
- **Runtime (`agent/mod.rs`, `providers.rs`, `roles.rs`, `tools.rs`,
  `fake.rs`):**
  - `ModelScale { Small, Large, Unknown }` en el trait (`≤ 9B` = Small, por
    nombre `:7b`/`:1.5b` o por `parameter_size` de `/api/show`);
    `RunConfig.toolset_compact` (por defecto `fs.read, fs.list, fs.patch,
    test.run`; cada `RoleSpec` trae el suyo) elegido cuando el proveedor es
    `Small`, con nota en el run.
  - `finish_structured(text, schema)` en el trait: Ollama con `format:
    <schema>`, OpenAI-compat con `response_format: json_schema`. El runtime
    lo pide cuando, agotado el recordatorio, el modelo sigue en prosa; la
    respuesta se registra como `finalizar` (`result_json`) y el run termina
    `Finished`. Si el proveedor no sabe o falla → `ModelStopped` como antes.
  - Reintento gratuito: el primer rechazo por argumentos (`ARGS_REJECT_
    PREFIX`) de cada herramienta devuelve el esquema de entrada y no
    consume paso; los rechazos de política no.
  - **`AgentStopReason::Looping`** (protocolo, barra, eval): la misma
    llamada (herramienta + argumentos) fallando tres veces seguidas corta
    el run; a la segunda el modelo recibe la advertencia. No estaba en el
    ticket: lo destapó la observación de un run real (18 `fs.patch`
    idénticos con un `old` inexistente).
  - Prompts (`system_prompt()`, `prompts/{coder,architect,auditor}.txt`)
    reescritos para 7B: «actúas SOLO con herramientas; nunca pidas el
    fichero al usuario, léelo con fs.read», ejemplo de llamada, método
    numerado, «no repitas una llamada que ya falló igual», `finalizar` al
    principio y al final.
- **Evaluación (`agent/eval.rs`, `cli/commands/eval.rs`):** `--repeat N`,
  `aggregate` (tasa, medianas, `passed` solo si todas), `CaseResult.repeat/
  successes` (JSON anterior se lee igual), `diff_with_margin` (tasas cuando
  hay repeticiones, `DEFAULT_RATE_MARGIN = 0.2`; los checks binarios y el
  +30 % se mantienen sin repeticiones), `antos eval diff --margin`,
  `RunOptions` (clippy: `run_all` superaba los 7 argumentos).
- **`.github/workflows/agents-nightly.yml`:** `schedule` + `workflow_dispatch`
  (modelo y repeticiones como inputs); instala Ollama con su instalador
  oficial (el runner no tiene `nix`), lo arranca con `antos service up
  ollama` (T34.1, `OLLAMA_MODELS` cacheable por `actions/cache`), `antos llm
  pull`, `eval --live --repeat 5`, artefacto con el JSON; no falla por tasa.
- **Docs:** manual §4.2 (perfiles, qué hace el runtime por un modelo
  pequeño, tabla de medidas, `--repeat`) y §4.13; `evals/agent/README.md`.
- **Tests de infraestructura que hubo que arreglar** (los nuevos tests que
  abren puertos y lanzan procesos los destaparon): `web::tests::t31_2_free_
  local_port` elegía el siguiente efímero del kernel (colisión con cualquier
  `bind(:0)` paralelo) → puerto alto pseudoaleatorio; `eval::run_case`
  nombraba el temporal con milisegundos (dos tests del mismo caso se
  pisaban el fixture) → contador atómico.

### 2. Medido (Mac 18 GB, `qwen2.5-coder:7b`, ctx 16k, temperatura 0.3, n=5)

| Estado | Rust «arregla el test» | Node «haz que pase test.js» |
|---|---|---|
| T34.2 | 3/5 | — |
| + toolset compacto, reintento, cierre estructurado, corte de bucle (prompts antiguos) | 2/5 | — |
| + prompts para 7B | **5/5**, y otra vez **5/5** (10/10, mediana 5 pasos, 7–11 s) | **0/5** |

Antes de los prompts, los fallos observados eran: (a) el modelo pide al
usuario el contenido del fichero en vez de leerlo (2 de 3 runs observados),
y (b) 18 `fs.patch` idénticos con un `old` que no existe. (b) lo corta el
runtime (`Looping`); (a) lo arregló el prompt. **Node 0/5** tiene una causa
concreta y distinta: el objetivo nombra `test.js` y el 7B empieza editando
el test (añade un mensaje al `assert`, luego lo quita) antes de mirar
`sum.js`; con 10 pasos corrige `sum.js` en el paso 9 y no le queda sitio,
o cierra antes con la suite en rojo. Es seguimiento de instrucción («nunca
modifiques un test»), no runtime; queda como la siguiente medida a atacar.
`eval diff` entre las dos ejecuciones ×5: «sin regresiones».

**Pipeline completo sin claves:** `antos llm profile local` (16–31 GB →
7b/7b/3b/7b) y `antos -y agent run T99.1 --auto` sobre un workspace con el
fixture y un ticket: Arquitecto (2 pasos, 9 s) → Coder (4 pasos, 15 s) →
QA en verde → Auditor aprueba (riesgo low) → **Ready for Approval en 32 s**,
todo con `ollama:qwen2.5-coder:7b` y «contexto: 16384 tokens».

### 3. Verificación ejecutada

- `cargo test --workspace`: 423 en `antosd` (+7: compacto, cierre
  estructurado, reintento gratuito, escala de modelo, bucle, perfiles,
  tasas/agregado), 0 fallos, dos ejecuciones seguidas; `clippy
  --all-targets -D warnings` y `fmt` limpios. `antos eval agent`
  (determinista, binario real, Seatbelt): 4/4.
- La barra (`session.rs`) solo añade `R::Looping` a un `match`; no compila
  en este Mac (memoria del proyecto), lo verifica `clippy-barra` en CI.

### Criterios de aceptación — estado

- ✅ `profile local` deja los cuatro roles en local con modelos que caben;
  `agent run --auto` llega a `ReadyForApproval` sin clave (real, 32 s).
- ✅ Test: config limpia + `auto` → `get_role_model("architect")` local
  (`ollama`); bajo `cloud`, el valor de antes.
- ✅ Test: cierre estructurado por `format`/`finish_structured`; reintento
  sin consumir paso ante argumento malformado; sin reintento por política.
- ✅ Test: el Coder con modelo pequeño recibe el toolset compacto; grande o
  desconocido, el completo.
- ✅ `eval --live --repeat 5` produce tasas; `diff` con 5/5 → 2/5 señala y
  5/5 → 4/5 no (test y real).
- ✅ Fiabilidad Rust n=5 antes/después anotada; objetivo ≥ 4/5 alcanzado
  (5/5, 10/10). Node 0/5 documentado con causa.
- ⏳ Job nocturno en verde al menos una vez: escrito con
  `workflow_dispatch`; no se puede ejecutar desde local. Pendiente de la
  primera ejecución en GitHub.
- ✅ Tests, clippy, fmt.
