# T28.5 · Ergonomía de Entrada: LEDs de Teclado, Auto-Repeat, Layouts y Aceleración de Puntero

> **Estado:** ✅ Completado
> **Fase:** Fase 28 (Periféricos Completos y Robustez de Plataforma Bare-Metal)
> **Fecha:** September 2026
> **Dependencias:** **T28.4** (decodificador HID) o **T28.1** (virtio-input) para que lleguen eventos.

## Descripción

Una vez que el kernel recibe eventos de teclado y ratón de forma fiable, faltan las conductas que un usuario espera de un sistema "completo": el LED de Bloq Mayús, la repetición automática al mantener una tecla, distribuciones de teclado distintas de US, y una curva de sensibilidad/aceleración para el puntero relativo (el "ratón demasiado lento" reportado venía de aplicar `dx`/`dy` crudos sin ninguna curva).

## Alcance Técnico

1. **LEDs de teclado:**
   - USB HID: `SET_REPORT(Output)` por control transfer con el bitmap de LEDs (`Num Lock`, `Caps Lock`, `Scroll Lock`) cada vez que `KeyboardState` cambia esos estados (`kernel/src/drivers/usb/xhci/mod.rs` + `kernel/src/input/mod.rs`).
   - VirtIO-Input: `EV_LED` por `statusq` (coordina con **T28.1**).

2. **Auto-repeat typematic (`kernel/src/input/mod.rs`):**
   - Estado: última tecla no-modificadora presionada + marca de tiempo (`crate::arch::…::timer::ticks()`).
   - *Delay* inicial (~500 ms) y *rate* (~30/s) configurables.
   - Emitir `InputEvent::KeyPress` repetidos mientras la tecla siga presionada y no haya llegado su `KeyRelease`.
   - Conducido desde el poll de entrada o desde el *tick* del timer (**T28.6**).

3. **Layouts de teclado (`kernel/src/input/mod.rs`):**
   - Extraer `KeyboardState::key_to_char` a una tabla de *layout* seleccionable.
   - Incluir al menos **US** y **ES** (teclas muertas básicas: acentos; `AltGr` para `@`, `#`, `€`, `~`, `\`).
   - Selección por variable de arranque, `/etc/antos.conf` o una syscall de configuración.

4. **Aceleración / sensibilidad de puntero (`kernel/src/ui/cursor.rs`):**
   - `move_rel`: curva configurable (ganancia lineal + umbral de aceleración: por debajo del umbral, factor 1; por encima, factor `> 1` proporcional a la magnitud del delta).
   - Parámetro de sensibilidad global; el *clamp* a los límites de pantalla se mantiene.
   - El modo absoluto (`move_abs`) no se ve afectado.

5. **Configuración en runtime:**
   - Exponer *layout*, sensibilidad y parámetros de *typematic* vía `SYS_SYSINFO` (lectura) y una vía de escritura (syscall de *settings* o relectura de `/etc/antos.conf`).

## Criterios de Aceptación

- Mantener pulsada una tecla imprimible repite el carácter tras el *delay* inicial, a *rate* constante, y se detiene exactamente al soltar (llega un solo `KeyRelease`).
- Bloq Mayús y Num Lock encienden el LED correspondiente del teclado emulado, tanto por USB HID como por VirtIO-Input.
- Con *layout* `ES` seleccionado, `ñ`, `ç`, `;`/`:`, y `AltGr` producen los caracteres correctos; con `US` el comportamiento actual no cambia.
- El puntero en modo relativo (`usb-mouse`) recorre toda la pantalla con un gesto normal de ratón; la sensibilidad es ajustable y el cambio se refleja sin reiniciar.
- Pruebas unitarias de la máquina de estados de auto-repeat, de las tablas de *layout* (US y ES) y de la curva de aceleración pasan con `cargo test --workspace`.

## Estado de Verificación (September 2026)

**Implementado:**

1. **Núcleo puro** (`kernel/src/input/ergonomics.rs`, nuevo):
   - `KeyboardLayout::{Us, Es}` + `resolve_key(layout, key, shift, caps, altgr)
     -> KeyResolve::{Char, Dead, None}`. US es un port fiel de la tabla anterior;
     ES añade `ñ`/`Ñ` (tecla US-`;`), `ç`/`Ç` (tecla US-`\`), `;`/`:` en las
     teclas de coma/punto, tercer nivel `AltGr` (`@ # € ~ \ | [ ] { }`) y teclas
     muertas (`´`, `` ` ``, `^`, `¨`). `combine_dead` compone `á é í ó ú ü ñ …`;
     `dead_key_glyph` da el glifo suelto.
   - `AutoRepeat` (ticks): `on_press` (una tecla distinta reinicia el *delay*
     inicial; el eco de la misma tecla no), `on_release` (sólo la tecla activa),
     `tick(now)` (emite tras el *delay*, luego cada *interval*), `set_timing`.
   - `PointerAccel { sensitivity_pct, threshold, accel_pct }` + `apply(delta)`:
     ganancia base + `max(0, |δ|-umbral) * accel_pct`, con suelo de 1 px para que
     el movimiento lento no se anule; conserva el signo.
   - `led_bitmap(num, caps, scroll)` → byte de LEDs HID.
   - 12 tests `#[cfg(test)]`.

2. **LEDs de teclado** (`input/mod.rs`, `drivers/usb/mod.rs`,
   `drivers/usb/xhci/mod.rs`):
   - `Modifiers` gana `num_lock`, `scroll_lock`, `altgr`. `KeyboardState::update`
     conmuta los tres *locks* y publica el bitmap deseado en un `AtomicU8`.
   - `sync_keyboard_leds()` empuja el bitmap sólo cuando cambia: VirtIO-Input vía
     `set_keyboard_leds` (T28.1) y USB vía `XhciController::push_keyboard_leds`
     (`SET_REPORT(Output)` en EP0 por interfaz). `KeyCode::{NumLock, ScrollLock}`
     nuevos, mapeados en `hid_usage_to_key` (`0x53`/`0x47`) y `linux_code_to_key`
     (`69`/`70`).

3. **Auto-repeat**: `AUTO_REPEAT: SpinLock<AutoRepeat>`. `note_key_for_repeat`
   alimenta la máquina desde `drain_ascii`; `service_auto_repeat(now)` reinyecta
   `InputEvent::KeyPress` sintéticos y se llama desde `drain_ascii` y desde el
   `halt_loop` (aarch64). Se detiene exactamente con el `KeyRelease` de la tecla.

4. **Layouts**: `KeyboardState::key_to_char` es ahora `&mut self`, resuelve por
   `resolve_key` según `keyboard_layout()` y arrastra la tecla muerta pendiente
   entre dos pulsaciones. `drain_ascii` emite **UTF-8** (antes descartaba lo no
   ASCII), así que `ñ`, `€`, `á` llegan al shell.

5. **Aceleración de puntero** (`ui/cursor.rs`): `Cursor::move_rel` aplica
   `input::pointer_accel().apply()` a `dx`/`dy` antes del *clamp*; `move_abs`
   queda intacto.

6. **Configuración en runtime** (`input/mod.rs`):
   - `INPUT_SETTINGS: SpinLock<InputSettings>` (layout, curva de puntero,
     *typematic*). `apply_config(&str)` parsea `keyboard_layout`,
     `pointer_sensitivity`, `pointer_accel`, `repeat_delay_ms`, `repeat_rate_hz`
     de `/etc/antos.conf` (leído en `main.rs`; `kernel/build.rs` añade esas
     claves al `antos.conf` del initrd).
   - `set_keyboard_layout`, `set_pointer_sensitivity` para cambio en caliente.
   - `settings_report()` se añade a `SYS_SYSINFO` (lectura).

**Pruebas:**
- `cargo build` del kernel para `x86_64-unknown-none` y `aarch64-unknown-none`:
  limpio.
- `cargo clippy` (kernel, x86_64): 44 advertencias (sin regresión respecto a
  T28.4).
- `cargo test --workspace` (crates host): en verde.
- `ergonomics.rs` (12 tests) validado con un binario `--test` equivalente
  (12/12), ya que el crate `no_std` del kernel no arranca bajo `cargo test`:
  tabla US sin cambios; ES (`ñ Ñ ç Ç ; :`), tercer nivel `AltGr` (`@ # € ~ \`),
  dead-acute (`á Ó`, `ü`, `ñ`); auto-repeat (espera el *delay*, repite a *rate*,
  para en el `KeyRelease` correcto, la tecla más nueva gana, el eco no reinicia);
  curva de aceleración (identidad bajo umbral, amplifica deltas grandes, suelo de
  1 px). Tests adicionales en `input/mod.rs` para `apply_config` y el bitmap de
  LEDs.

**Pendiente de verificación fuera del CI headless:** LED físico encendiéndose en
el teclado emulado y sensación real de repetición / aceleración en QEMU/UTM. La
lógica pura está cubierta por tests; la integración comparte las rutas de
`control_transfer` (T28.3) y `virtio_input::set_keyboard_leds` (T28.1) ya
verificadas.
