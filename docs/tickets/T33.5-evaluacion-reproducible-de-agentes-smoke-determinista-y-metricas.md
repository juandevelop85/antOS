# T33.5 · Evaluación Reproducible de Agentes: Smoke Determinista y Métricas

> **Estado:** ✅ Completado
> **Fase:** Fase 33 (Agentes que construyen software)
> **Prioridad:** 🟡 Media
> **Dependencias:** T33.2, T33.3, T31.10 (CI verde como requisito),
> T22.x (bench e historial).
> **Origen:** Un runtime de agentes sin evaluación se degrada sin que nadie
> lo note: un cambio de prompt, de modelo o de toolset puede duplicar los
> pasos o dejar de pasar tests y nada en CI lo diría.

## Descripción

Dos niveles de evaluación, ambos declarativos y guardados en el repo:

1. **Smoke determinista (CI, siempre):** casos en `evals/agent/*.toml`
   con un proyecto de ejemplo (fixture), un objetivo y un **guion** del
   proveedor `fake` (T33.2). Verifica el *contrato* del runtime: orden de
   herramientas, validación, tiers, snapshots, journal, presupuesto,
   informe. Corre en `cargo test` y en el job `nix-build`.
2. **Evaluación con modelo (opcional, `antos eval agent --live`):** los
   mismos casos contra el proveedor configurado, con métricas por caso:
   pasos, tokens/coste, tiempo, tests en verde (sí/no), ficheros tocados
   fuera del plan, rechazos de validación. Resultados en
   `.antos/evals/<fecha>.json` y `antos eval diff` contra la ejecución
   anterior (reutiliza el enfoque de `bench.diff`, T22.x). Nunca en CI
   (clave y coste); sí documentado como paso previo a cambiar prompts o
   modelos por rol.

## Alcance Técnico

- **`evals/agent/`:** fixtures pequeños (un crate Rust con un test
  rojo; un proyecto Node con un test rojo) y casos `.toml` (`goal`,
  `toolset`, `budget`, `script` para `fake`, `expect` = herramientas
  esperadas, tests en verde, ficheros permitidos).
- **`system/antosd/src/agent/eval.rs`:** carga de casos, ejecución con
  `fake` o proveedor real, cálculo de métricas, diff contra la ejecución
  anterior con umbrales (p. ej. +30 % pasos o cualquier test en rojo →
  regresión).
- **CLI:** `antos eval agent [--live] [--case <nombre>]`, `antos eval diff`.
- **CI (`.github/workflows/ci.yml`):** job que ejecuta el smoke `fake`.
- **Docs:** `docs/manual-de-comandos.md` (sección de agentes: cómo
  configurar proveedor y modelos por rol, cómo evaluar antes de cambiar
  prompts).

## Criterios de Aceptación

- `antos eval agent` (sin `--live`) pasa en CI con todos los casos y
  falla si se rompe el orden de herramientas, la validación o el
  presupuesto (test que muta un caso y comprueba el fallo).
- `antos eval agent --live` con clave real produce el JSON de métricas y
  `antos eval diff` señala una regresión inyectada (test con dos JSON de
  ejemplo).
- El manual documenta el flujo completo: proveedor → modelos por rol →
  `antos agent do` → `antos agent run <ticket> --auto` → aprobación →
  PR, y cómo evaluar.
- `cargo test --workspace` y CI en verde.

## Estado de Verificación

### 1. Los cambios

- **`evals/agent/`:** README con el formato; fixtures `rust-red-test`
  (crate con `sum` que multiplica y un test en rojo; excluido del workspace
  en `Cargo.toml`) y `node-red-test` (`npm test` → `node test.js`); cuatro
  casos: `rust-fix-failing-test` (read → patch → test.run → finalizar,
  tests en verde, solo `src/lib.rs`), `node-fix-failing-test` (igual con
  npm; `requires = ["npm", "node"]`), `rust-rejects-out-of-scope` (tres
  rechazos del catálogo/toolset/escape, sin tocar disco) y
  `budget-stops-the-run` (`budget_exhausted` a los 2 pasos).
- **`system/antosd/src/agent/eval.rs`:** `EvalCase`/`Expectation` (TOML),
  `run_case` (fixture copiado a un temporal **canónico** — en macOS
  `temp_dir()` es `/var/folders/…`, enlace a `/private/var/…`, y Seatbelt
  compara rutas reales: sin canonicalizar, cada escritura del agente era
  «Operation not permitted» aunque estuviera declarada), veredicto (orden
  de herramientas en modo determinista, rechazos, motivo de parada,
  ficheros permitidos, suite en verde al terminar), `run_all`, `save`
  (`<state>/evals/<fecha>.json`), `last_two`, `diff` (pasaba→falla,
  verde→rojo, `finished`→otro, +30 % pasos o tokens, caso desaparecido).
- **CLI:** `antos eval agent [--live] [--provider p] [--case n]` (ejecutor
  confinado) y `antos eval diff`.
- **CI:** paso «Deterministic agent evaluation» en el job del escritorio
  Linux (`target/release/antos eval agent`, Landlock).
- **Docs:** manual §4.2 con el flujo evaluar → cambiar → evaluar → diff.

### 2. Verificación ejecutada

- `cargo test --workspace`: 472, 0 fallos. Nuevos (`agent::eval::tests`):
  los casos del repo pasan en proceso con `fake`; un caso con expectativa
  mutada (orden y motivo) falla con exactamente dos hallazgos; `diff`
  ignora +20 % de pasos y señala cuatro regresiones inyectadas y un caso
  desaparecido; `save`/`last_two` ida y vuelta.
- `cargo clippy --workspace --all-targets -- -D warnings`: 0; `fmt` limpio.
- **Binario real, ejecutor confinado (Seatbelt):** `antos eval agent` →
  4/4 (`budget_exhausted` a 2 pasos; Node y Rust con tests verdes tras el
  parche; 3 rechazos sin tocar disco). `antos eval diff` entre dos
  ejecuciones → «sin regresiones».
- Con clave real (`--live`): no ejecutado (coste); el camino es el mismo
  con `providers::resolve`.
- Bajo Landlock (CI) el paso `test.run` *dentro* del run puede quedar en
  rojo por la limitación conocida de T33.2 (`/nix/store`, `~/.cargo` y
  `~/.rustup` no legibles); la suite final del fixture la ejecuta el
  evaluador fuera del recinto, así que el veredicto sigue siendo válido.

### Criterios de aceptación — estado

- ✅ `antos eval agent` pasa con todos los casos y falla ante un caso
  mutado (test).
- ⏳ `--live` con clave real: pendiente de una sesión con proveedor; `diff`
  sobre dos JSON verificado (test y ejecución real con `fake`).
- ✅ Manual: proveedor → modelos por rol → `agent do` → `agent run --auto`
  → aprobación → evaluar.
- ✅ Tests y CI en verde.
