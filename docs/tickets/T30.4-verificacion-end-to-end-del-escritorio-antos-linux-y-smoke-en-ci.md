# T30.4 · Verificación End-to-End del Escritorio antOS Linux y Smoke en CI

> **Estado:** 🔄 En Progreso
> **Fase:** Fase 30 (Escritorio antOS Linux — NixOS / Wayland, driver diario)
> **Fecha:** September 2026
> **Dependencias:** T30.1, T30.2, T30.3.

## Descripción

Con la imagen gráfica y el userland en su sitio, hace falta comprobar de punta a
punta que el escritorio antOS Linux **funciona de verdad**: la barra se ancla,
los atajos hacen lo que dicen, `antosd` está vivo y la barra habla con él en
tiempo real, y la captura de pantalla para QA visual opera. Y dejarlo cubierto
por un *smoke* automatizado en CI con un compositor *headless*.

## Alcance Técnico

1. **Checklist de verificación manual (documentado en
   `docs/guia-emulacion-utm-virtualbox.md`):**
   - `antos-barra` se ancla por `wlr-layer-shell` en el borde configurado y
     recibe foco de teclado en modo *on-demand*.
   - Atajos: `Super+Space` (barra de intención), `Super+A` (panel de agentes /
     tablero Kanban), `Super+Return` (terminal), `Super+W` (`antos dev`),
     `Super+Q` (cerrar ventana) — todos operativos.
   - `antosd` arranca como servicio (systemd del sistema o de usuario) antes
     que la barra; `systemctl status antos*` en verde.
   - Escribir una intención en la barra la envía por IPC a `antosd` y la
     respuesta/estado del equipo `antFlow` se refleja en el panel **en vivo**
     (evento `FlowTransition` → repintado reactivo, T20.1).
   - `grim`/`slurp` (o `wlr-screencopy` directo) capturan un *buffer* del
     escritorio para el flujo de QA visual (T14.2).

2. **Smoke automatizado en CI (`.github/workflows/`):**
   - *Job* `antos-linux-desktop`: en un *runner* Linux, arranca Labwc en modo
     *headless* (`labwc` con backend `wlroots` *headless*, o `cage`/`wlheadless`
     + `WLR_BACKENDS=headless`), lanza `antosd` + `antos-barra`.
   - Verifica: la barra crea su superficie layer-shell (log/`wayland-info`),
     `antosd` responde a un *ping* IPC, y una captura `grim` produce un PNG no
     vacío.
   - Falla por *timeout* si la superficie no aparece o el IPC no responde.

3. **Test de integración host (`system/barra` / `system/antosd`):** enviar una
   intención sintética por el canal IPC y comprobar que `antosd` la despacha y
   emite el evento de estado que la barra consumiría (sin GUI, a nivel de
   protocolo).

## Criterios de Aceptación

- El checklist manual pasa entero en la VM gráfica de T30.2 (barra anclada,
  los 5 atajos, `antosd` vivo, IPC barra↔demonio en vivo, captura de pantalla).
- El *job* `antos-linux-desktop` de CI arranca el compositor *headless* +
  `antosd` + `antos-barra`, verifica la superficie layer-shell, el *ping* IPC
  y una captura `grim` no vacía, y **pasa**.
- Un test de integración host envía una intención por IPC y comprueba el
  despacho + evento de estado.
- Cualquier fallo (superficie ausente, IPC mudo, captura vacía) hace fallar el
  *job* con un mensaje claro, sin *traceback*.
- `cargo test --workspace` en verde; `cargo clippy` sin regresión.
- Documentado el checklist y el *job* de CI.

## Estado de Verificación (September 2026)

**Implementado:**

1. **Test de integración host** (`system/antosd/tests/desktop_ipc.rs`, **nuevo**,
   sin GUI): arranca `antos demonio` con workspace/estado temporales y:
   - `daemon_dispatches_intent_and_streams_events_to_the_bar` — envía
     `Request::Intent { planner: "local", dry_run: true }` por el socket UNIX
     (igual que `antos-barra`) y comprueba que el primer `Event` es
     `Start { intent, planner: "local" }` y que la sesión llega a un evento
     terminal (`Result`/`Error`/`Proposal`/`Note`) y cierra limpia; falla si
     algún `Event` es ilegible para la barra.
   - `daemon_serves_git_status_query_for_the_bar_badges` — `QueryGitStatus`
     responde `GitStatus` o `NotGitRepo` (la insignia de git de la barra).
   - Nombre de directorio temporal **corto** a propósito: la ruta del socket
     UNIX no puede pasar de `SUN_LEN`.
2. **Smoke headless** (`system/desktop/smoke.sh`, **nuevo**): arranca `labwc`
   con backend headless (`WLR_BACKENDS=headless`, `WLR_RENDERER=pixman`) +
   `antos demonio` + `antos-barra` y verifica, con mensajes claros y salida
   `!= 0` a la primera:
   - el compositor anuncia `zwlr_layer_shell_v1` (`wayland-info`),
   - el socket IPC responde a `QueryGitStatus`,
   - `antos-barra` sigue vivo 3 s tras crear su superficie,
   - `grim` captura un PNG > 1 KiB.
   Imprime `SMOKE OK` / `SMOKE FALLO: <causa>`; limpia sus procesos y su
   directorio al salir (`trap`).
3. **Job de CI** `antos-linux-desktop` (`.github/workflows/ci.yml`):
   `ubuntu-latest`, instala `labwc grim wayland-utils libgtk-4-dev
   libgtk4-layer-shell-dev`, compila `antosd` (`--release`) y
   `system/barra` (`--release`), y ejecuta `system/desktop/smoke.sh` con
   `ANTOS_BIN`/`ANTOS_BARRA_BIN` a los binarios recién construidos.
4. **Documentación**: `docs/manual-de-comandos.md` (Método 5 · T30.4) con el
   smoke, la integración host y el checklist manual de 8 puntos; puntero
   desde `docs/guia-emulacion-utm-virtualbox.md`.

**Pruebas (macOS, este entorno):**

- `cargo test -p antosd --test desktop_ipc` → **2/2 OK**.
- `cargo test --workspace` → **317/317** (315 previos + 2 nuevos), 0 fallos.
- `cargo clippy --workspace --tests` → `desktop_ipc.rs` sin avisos; el total
  (97) es todo preexistente en `antosd`/`flow`.
- `bash -n system/desktop/smoke.sh` OK; ejecutado en macOS falla como debe:
  `SMOKE FALLO: labwc no está en el PATH`, salida 1, sin *traceback*.
- Estructura del *job* de CI verificada (indentación, sin tabs, 9 jobs).

**Pendiente de verificación en un runner Linux:**

- Ejecución real del *job* `antos-linux-desktop` (labwc headless + barra +
  `grim`) — necesita `ubuntu-latest` con Wayland; se validará al hacer *push*.
- El checklist manual de 8 puntos sobre la VM gráfica de T30.2 (arranque a la
  sesión, atajos `Super+*`, IPC barra↔demonio en vivo, `nvim`+`antos_lsp`).

Queda en **🔄 En Progreso**: la integración host está **verificada aquí** (2
tests en verde); el *smoke* headless y el checklist necesitan un Linux con
Wayland.
