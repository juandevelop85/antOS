# T31.8 · Límites y Tiempos de Espera en el Socket IPC del Demonio

> **Estado:** ✅ Completado
> **Fase:** Fase 31 (Auditoría de Código: Corrección y Endurecimiento)
> **Prioridad:** 🟡 Media
> **Fecha:** September 2026
> **Dependencias:** T22.1 (modularización de `antosd`), T4.1 (barra de intenciones).
> **Origen:** Auditoría de código de September 2026.

## Descripción

`system/antosd/src/ipc.rs` atiende el socket Unix por el que hablan el CLI y la
barra Wayland. La cabecera del módulo dice: «El socket vive en el directorio de
estado con permisos 0600. Esa es hoy [la frontera]». Esa frontera tiene tres
grietas.

### Defecto 1 · Lectura de línea sin techo

`receive` (`ipc.rs:62-69`) usa `BufRead::read_line` sobre un `BufReader` sin
límite:

```rust
let mut line = String::new();
if source.read_line(&mut line)? == 0 {
    return Ok(None);
}
```

Un cliente que envíe bytes sin `\n` hace crecer la cadena hasta agotar la
memoria del demonio. El formato es una línea de JSON por mensaje, así que basta
envolver el lector en `Read::take(MAX)` y responder con un error de protocolo al
superarlo.

### Defecto 2 · Sin tiempos de espera y con atención estrictamente serial

`servir` (`ipc.rs:133-148`) atiende una conexión cada vez, y el comentario
explica bien por qué: dos intenciones mutando el mismo espacio de trabajo
producirían diffs que no describen el resultado. La serialización es correcta;
lo que falta es un tiempo de espera. Sin `set_read_timeout`, un cliente que
conecta y no envía nada bloquea el bucle **para todos los demás clientes**,
indefinidamente. Con permisos `0600` el atacante tiene que ser el propio
usuario, así que no es una escalada, pero sí un bloqueo trivial y accidental:
una barra Wayland colgada deja el CLI inservible.

### Defecto 3 · Ventana entre `bind` y `chmod`

`ipc.rs:123-125`:

```rust
let listener = UnixListener::bind(&path)...;
std::fs::set_permissions(&path, std::fs::Permissions::from_mode(0o600))?;
```

Entre las dos llamadas el socket existe con los permisos que dicte la `umask`.
En un directorio de estado compartido, otro usuario local puede conectarse en
esa ventana. Se cierra fijando la `umask` antes del `bind` y restaurándola
después, o creando el socket dentro de un directorio ya restringido a `0700`.

### Defecto 4 · Marca `syso` en la salida

`ipc.rs:129` sigue imprimiendo `syso · demonio escuchando en`, pese a que T0.1
dio por cerrado el renombrado. Se aborda de forma completa en T31.11.

## Alcance Técnico

1. Acotar el mensaje con `take(MAX_MESSAGE_BYTES)` y devolver un `Event::Error`
   descriptivo cuando se exceda, sin cerrar el demonio.
2. Aplicar `set_read_timeout` y `set_write_timeout` a cada `UnixStream`
   aceptado, y registrar en la bitácora las conexiones abandonadas.
3. Restringir el directorio de estado a `0700` y crear el socket dentro, o
   ajustar la `umask` alrededor del `bind`.
4. Documentar en la cabecera del módulo que la serialización de conexiones es
   deliberada, para que nadie la «arregle» convirtiéndola en concurrente.

## Criterios de Aceptación

- Un test envía un mensaje que supera el límite y recibe un error de protocolo;
  la memoria residente del demonio no crece de forma apreciable.
- Un test abre una conexión, no envía nada, y comprueba que una segunda
  conexión es atendida dentro del tiempo de espera configurado.
- El socket nunca es accesible a otros usuarios, ni siquiera transitoriamente,
  verificado con una `umask` permisiva en el test.
- `cargo test --workspace` en verde.

## Estado de Verificación (September 2026)

**Implementado** en [`system/antosd/src/ipc.rs`](file:///Users/juandevelop/Develop/antOS/system/antosd/src/ipc.rs)
(el Defecto 4, la marca `syso` en la salida, queda explícitamente fuera de
alcance de este ticket — se resuelve en T31.11, tal como el propio ticket
indicaba):

1. **Lectura acotada.** `receive` ahora lee con
   `source.by_ref().take(MAX_MESSAGE_BYTES + 1).read_line(...)`
   (`MAX_MESSAGE_BYTES = 4 MiB`), y devuelve un nuevo `enum Received<T>`
   (`Message`/`Eof`/`TooLarge`) en vez de `Option<T>` — leer un byte de más
   que el límite es lo que permite distinguir «la línea termina justo en el
   límite» de «la línea es más larga y se truncó ahí». `handle_connection`
   responde a `TooLarge` con `Event::Error(...)` antes de cerrar la
   conexión; `on_proposal` trata `TooLarge` igual que ausencia de respuesta
   («el silencio nunca es un sí»). El único otro llamador de `receive`
   —`intencion_remota`, el lado cliente, fuera del alcance de seguridad de
   este ticket pero que comparte la firma— se adaptó al nuevo tipo sin
   cambiar su comportamiento.
2. **Tiempos de espera, con el reloj correcto para cada fase.** `servir`
   aplica `set_read_timeout`/`set_write_timeout` (10 s cada uno) a cada
   `UnixStream` aceptado, antes de despachar. En cuanto `handle_connection`
   recibe una petición real (`Received::Message`), libera el tiempo de
   espera de lectura (`writer.set_read_timeout(None)`) — necesario porque
   una aprobación humana interactiva puede tardar más de 10 segundos, y el
   ticket no pedía romper eso, solo acotar la espera de una conexión que
   nunca llega a enviar nada. El tiempo de espera de escritura se mantiene
   activo durante toda la conexión. Nueva `is_idle_timeout` reconoce el
   error de E/S del tiempo de espera y lo registra de forma distinguible:
   «conexión IPC abandonada: sin actividad durante …, cerrada», en vez del
   mensaje genérico de error de sesión.
3. **Sin ventana entre `bind` y `chmod`.** Elegida la opción que el propio
   ticket ofrecía como alternativa: «ajustar la `umask` alrededor del
   `bind`». Nueva `bind_socket_with_restrictive_umask` fija `umask(0o077)`
   antes de `UnixListener::bind` y restaura la anterior justo después; el
   fichero del socket nunca existe con más permisos que el propio usuario,
   ni siquiera transitoriamente. `bind_socket` añade el `chmod` explícito a
   `0600` como exactitud, no como la garantía real.
4. **Documentado en la cabecera del módulo.** Nuevo apartado «Una conexión
   cada vez, a propósito» en el `//!` de cabecera, explicando por qué la
   serialización de conexiones es deliberada (evita diffs que dejan de
   describir el resultado cuando dos intenciones mutan el mismo espacio de
   trabajo a la vez) y qué sí hacía falta arreglar (el tiempo de espera),
   para que nadie la «arregle» convirtiéndola en concurrente sin resolver
   antes esa cuestión de fondo.

**Pruebas añadidas** (4 nuevas, primer módulo de tests de `ipc.rs`):
`test_oversized_message_gets_a_protocol_error_not_a_hang` (criterio literal:
un mensaje de más de 4 MiB sin salto de línea recibe `Event::Error`, no un
cuelgue ni una desconexión sin respuesta — la garantía de memoria acotada es
estructural, por la lectura con `take`, no medida por RSS en el test, igual
que el razonamiento ya aplicado en T31.2 para una propiedad equivalente),
`test_second_connection_is_served_after_an_idle_first_connection_times_out`
(criterio literal: conexión A inactiva agota su tiempo de espera —200 ms en
el test, el mismo mecanismo `set_read_timeout` que usa `servir` en
producción con 10 s—, y la conexión B se sirve con normalidad justo
después), `test_socket_is_never_group_or_world_accessible_even_transiently`
(criterio literal: bajo `umask(0o000)`, el paso de `bind` envuelto en
`umask` por sí solo —sin el `chmod` posterior— ya deniega grupo/otros), y
`test_bind_socket_final_permissions_are_0600` (el resultado final completo
es exactamente `0600`).

**Verificado:**

- `cargo test --workspace` → **376/376** (372 previos + 4 nuevos), 0 fallos,
  estable en tres ejecuciones consecutivas.
- `cargo clippy -p antosd --all-targets -- -D clippy::unwrap_used
  -D clippy::expect_used` (la puerta de T31.7) sigue en verde: cero
  `unwrap()`/`expect()` fuera de test en el código nuevo.
- `cargo clippy -p antosd --all-targets` (línea base) → 86 avisos, idénticos
  a los previos al cambio.
- `cargo check --workspace` limpio.
