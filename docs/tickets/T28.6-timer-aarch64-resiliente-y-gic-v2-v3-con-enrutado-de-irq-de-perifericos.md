# T28.6 · Timer AArch64 Resiliente (Fallback Físico EL1) y GIC v2/v3 con Enrutado de IRQ de Periféricos

> **Estado:** ✅ Completado
> **Fase:** Fase 28 (Periféricos Completos y Robustez de Plataforma Bare-Metal)
> **Fecha:** September 2026
> **Dependencias:** ninguna. Beneficia a **T28.1**, **T28.3** y **T28.7** (atención por IRQ en vez de solo polling).

## Descripción

En VirtualBox ARM64 el contador de *ticks* del kernel se queda en `0` (`info` → `uptime_ticks: 0`): la IRQ del *timer virtual* (PPI 27, `CNTV_*`) no se dispara. Como el sondeo periódico de USB/entrada se apoya en ese *tick*, la máquina queda semi-inerte. En QEMU/UTM `-M virt` el *timer* sí funciona, pero solo con **GICv2**; el kernel no soporta **GICv3** (hardware real y algunas configuraciones de QEMU).

Este ticket hace el *timer* **resiliente** (fallback al *timer físico EL1*) y generaliza el **GIC** a v2 y v3, además de enrutar al GIC las IRQ de los periféricos (xHCI, VirtIO) para no depender exclusivamente del *polling* cooperativo.

## Alcance Técnico

1. **Timer resiliente (`kernel/src/arch/aarch64/timer.rs`):**
   - Al iniciar: leer `CNTVCT_EL0` en dos instantes separados por un *spin* acotado; si el valor no avanza, el *timer virtual* no está disponible.
   - Fallback al **timer físico EL1**: `CNTP_CTL_EL0`, `CNTP_TVAL_EL0` / `CNTP_CVAL_EL0`, IRQ **PPI 30** (INTID 30) en lugar de PPI 27.
   - `handle_timer_interrupt`: rearmar `CNTx_TVAL_EL0` en cada *tick* y hacer EOI correcto en el GIC.
   - `ticks()` monótono con independencia de la fuente; registrar en el log qué fuente se eligió.

2. **GIC v2/v3 (`kernel/src/arch/aarch64/gic.rs`):**
   - Detección de versión por `compatible` del DTB (`arm,gic-v3` vs `arm,cortex-a15-gic` / `arm,gic-400`).
   - **GICv3**: interfaz de CPU por registros de sistema (`ICC_SRE_EL1.SRE = 1`, `ICC_PMR_EL1`, `ICC_IGRPEN1_EL1`, `ICC_IAR1_EL1` / `ICC_EOIR1_EL1`) y redistribuidor (`GICR_WAKER`, `GICR_ISENABLER0` para PPIs/SGIs).
   - **GICv2** (actual) como camino alternativo.
   - API común: `enable_irq(intid, prio)`, `acknowledge() -> intid`, `end_of_interrupt(intid)`.

3. **Enrutado de IRQ de periféricos (`gic.rs` + `kernel/src/arch/aarch64/exceptions.rs`):**
   - Habilitar los SPI de: **xHCI** (del array `interrupts` del nodo PCI o del DTB), **VirtIO-Input MMIO** y **VirtIO-GPU MMIO**.
   - En `aarch64_exception_dispatch`: al recibir esas INTID, invocar `crate::drivers::usb::poll()` / `virtio_input::poll_virtio_inputs()` / servicio del *event ring*, además del *polling* cooperativo existente.

4. **Verificación de arranque (`kernel/src/main.rs`):**
   - Sustituir el bucle de "espera de pulsos con límite seguro" por la comprobación de fuente de *timer* + arranque real, y reportar `GIC vN` y `timer <virtual|físico>` en el log.

## Criterios de Aceptación

- En **VirtualBox ARM64**, `info` muestra `uptime_ticks` incrementando (hoy `0`), usando el *timer físico EL1*.
- En **QEMU/UTM `-M virt`** (GICv2) el *timer* sigue funcionando sin regresión.
- Con `-M virt,gic-version=3` el kernel arranca, el *timer* hace *tick* y las IRQ se atienden (EOI correcto, sin tormenta de interrupciones).
- Una IRQ de xHCI o de VirtIO-Input despierta el servicio del dispositivo **sin** depender del *polling* de `SYS_READ`.
- El log de arranque indica la versión de GIC y la fuente de *timer* detectadas.
- Pruebas unitarias del cálculo de recarga de `TVAL` y de la selección de fuente de *timer* pasan con `cargo test --workspace`.

## Estado de Verificación (September 2026)

**Implementado:**

1. **Timer resiliente** (`kernel/src/arch/aarch64/timer.rs`):
   - `TimerSource::{Virtual, Physical}`. `init()` arma el *timer virtual*
     (`CNTV_*`, PPI 27). `verify_and_fallback()` (llamado desde `main` con las
     IRQ ya desenmascaradas): comprueba que `CNTVCT_EL0` avanza y espera un
     *tick* acotado; si no llega, `switch_to_physical()` enmascara el virtual,
     arma `CNTP_TVAL_EL0`/`CNTP_CTL_EL0` y habilita la **PPI 30**.
   - `switch_to_physical()` envuelve el `msr cntp_tval_el0` en
     `exceptions::probe_guard` (nuevo): si el acceso a `CNTP_*` *trapea*
     (`EC 0x18`, huésped en EL1 cuyo EL2 no habilitó `CNTHCTL_EL2.EL1PCEN`) el
     kernel lo detecta y se queda en el virtual en vez de entrar en pánico.
   - `handle_timer_interrupt()` rearma el `TVAL` de la fuente activa.
     `active_irq()` devuelve 27 o 30; `is_timer_irq()` acepta ambas.
   - `reload_interval(freq, tick_hz)` y `select_source(bool)` puros y testeados;
     `uptime_seconds()` deriva de `CNTVCT_EL0` si aún no hubo *ticks*.

2. **GIC v2/v3** (`kernel/src/arch/aarch64/gic.rs`):
   - `GicVersion::{V2, V3}` + `gic_version_from_compatible(&[u8])` (puro,
     testeado). `set_version()` la fija desde `dtb::find_gic()`.
   - `init()` despacha: `init_v2()` (camino histórico intacto) o `init_v3()`
     (GICD ARE+Grp1 con espera de RWP; GICR: `GICR_WAKER` despierta el frame 0,
     `GICR_IGROUPR0`; interfaz de CPU por sistema: `ICC_SRE_EL1.SRE=1`,
     `ICC_PMR_EL1=0xF0`, `ICC_BPR1_EL1=0`, `ICC_IGRPEN1_EL1=1`).
   - API común `enable_interrupt(id)` / `acknowledge()` / `end_of_interrupt(id)`
     con despacho por versión; v3 usa `GICR_ISENABLER0` (PPI/SGI) o
     `GICD_ISENABLER` + `GICD_IGROUPR` + `GICD_IROUTER=0` (SPI) y
     `ICC_IAR1_EL1`/`ICC_EOIR1_EL1`. `is_spurious(1020..=1023)`.

3. **Enrutado de IRQ de periféricos**:
   - `gic::enable_peripheral_irqs()` habilita los SPI de QEMU `-M virt`: PCIe
     INTx (INTID 35..=38) y virtio-mmio (INTID 48..=79).
   - `aarch64_exception_dispatch`: descarta espurias sin EOI; para cualquier
     INTID no-timer llama `usb::poll()` + `virtio_input::poll_virtio_inputs()`
     (drena el dispositivo → la línea de nivel se desasserta antes del EOI, sin
     tormenta) y luego EOI.

4. **`dtb.rs`**: `find_gic() -> GicInfo { is_v3, gicd_base, second_base }` a
   partir del `compatible` y el `reg` (dos rangos de 16 B) del nodo GIC.

5. **`main.rs`**: detecta la versión de GIC por DTB antes de `gic::init()`,
   sustituye el bucle de "espera de pulsos con límite seguro" por
   `timer::verify_and_fallback()` y registra `GICvN` y la fuente de *timer*
   (`virtual` / `físico EL1`). `SYS_SYSINFO` añade `uptime_s`, `timer_src` y
   `gic`.

**Pruebas:**
- `cargo build` del kernel para `x86_64-unknown-none` y `aarch64-unknown-none`:
  limpio.
- `cargo clippy`: x86_64 44, aarch64 45 advertencias (sin regresión).
- `cargo test --workspace` (crates host): en verde.
- Módulos `#[cfg(test)]` en `timer.rs` (4) y `gic.rs` (3) validados con un
  binario `--test` equivalente (6/6): `reload_interval` (divide freq/tick, nunca
  0), `select_source`, `is_timer_irq` (27 y 30), `gic_version_from_compatible`
  (v3 en lista mixta, variantes v2, defecto v2), `is_spurious`.

**Pendiente de verificación fuera del CI headless:** arranque real en QEMU
`-M virt` (GICv2, sin regresión), `-M virt,gic-version=3` (bring-up GICv3, EOI
correcto) y VirtualBox ARM64 (fallback a *timer físico* y/o GICv3 según lo que
exponga su DTB; `uptime_ticks` incrementando). El camino GICv2 + *timer virtual*
—el verificado hasta T28.5— no cambia salvo que el DTB anuncie `arm,gic-v3`.
