# T36.4 · Perfil de Máquina Física: Red, Audio, Teclado y Contraseña en el Asistente

> **Estado:** 🔄 En Progreso
> **Fase:** Fase 36 (Instalable de verdad)
> **Prioridad:** 🟡 Media
> **Fecha:** September 2026
> **Dependencias:** T30.1 (`desktop.nix`), T30.9 (sabor Plasma), T36.1
> (`InstallConfig`, `configuration.nix` generado), T36.2 (*hook* de
> contraseña `set_user_password`).
> **Origen:** revisión del proceso de instalación (2026-09-21). No existe
> ningún perfil de máquina física en el repo: todos los
> `nixosConfigurations` son de VM —
> [`configuration.nix`](../../system/nixos/configuration.nix) importa
> `profiles/qemu-guest.nix`, autologin de **root** con contraseña `antos` y
> `console=ttyAMA0`; [`boot.nix`](../../system/nixos/boot.nix) usa
> `/dev/vda` — y `antos-desktop` («para hardware») hereda todo eso.
> [`desktop.nix`](../../system/nixos/desktop.nix) no declara red, audio,
> bluetooth, firmware ni layout de teclado Wayland (solo `console.keyMap`),
> y fija `initialPassword = "antos"` para un usuario en `wheel`; el
> asistente nunca pide contraseña.

## Descripción

Separar «la máquina antOS» de «la VM de desarrollo», y que la primera
traiga lo que un portátil necesita para ser usable el primer día: WiFi,
sonido, bluetooth, firmware, teclado en el idioma del usuario, locale y
zona horaria, y una contraseña que el usuario eligió. Todo declarativo,
en un módulo, para que el `configuration.nix` que genera el instalador
sea corto y legible.

## Alcance Técnico

1. **`system/nixos/machine.nix`** (módulo `services.antos.machine`,
   importado por `nucleo`, deshabilitado por defecto):
   - `enable`, `keyboardLayout` (`"us"`), `keyboardVariant` (`""`),
     `locale` (`"en_US.UTF-8"`), `timeZone` (`"UTC"`).
   - Al activarse: `networking.networkmanager.enable = true` (+ el usuario
     del escritorio en `networkmanager`), `hardware.enableRedistributableFirmware
     = true`, `services.pipewire` (`enable`, `alsa`, `pulse`, `wireplumber`)
     con `security.rtkit`, `hardware.bluetooth.enable = true` (+ `blueman`
     solo con sabor Labwc; Plasma trae el suyo), `services.xserver.xkb =
     { layout; variant }` (lo leen Plasma/KWin y `greetd`) y
     `environment.sessionVariables.XKB_DEFAULT_{LAYOUT,VARIANT}` (lo lee
     Labwc), `console.keyMap` derivado del layout, `i18n.defaultLocale`,
     `time.timeZone`, `security.sudo.wheelNeedsPassword = true`,
     `services.power-profiles-daemon.enable = true`, `zramSwap.enable =
     true`, `boot.loader.systemd-boot` + `efi.canTouchEfiVariables` (lo que
     hoy escribe el generador a mano), `services.fstrim.enable = true`.
   - Nada de `qemu-guest.nix`, `ttyAMA0`, ni autologin de root.
2. **`desktop.nix`:** `initialPassword` deja de tener valor por defecto
   (`lib.mkDefault null` → NixOS no crea contraseña; la VM y la ISO la
   ponen explícitamente en sus ficheros). La ISO en vivo conserva el
   autologin sin contraseña (es un live); la VM conserva `antos`.
3. **VM separada de máquina:** `configuration.nix` se parte en
   `vm-common.nix` (qemu-guest, serie, root autologin, contraseña `antos`)
   y lo mínimo común. `nixosConfigurations.antos-desktop-<system>` (una
   por arquitectura, T36.3 la usa para la closure) = `nucleo` +
   `services.antos.desktop.enable` + `services.antos.machine.enable`, sin
   `vm-common.nix`. `antos-vm`/`antos-desktop-vm` no cambian de
   comportamiento (mismo `toplevel.drvPath` antes y después, salvo el
   reparto de ficheros).
4. **El asistente pide contraseña, teclado, locale y zona horaria:**
   - Contraseña dos veces sin eco (`libc::tcgetattr`/`tcsetattr` sobre
     `stdin`; `libc` ya está en el árbol; sin `stty` ni `sh -c`), mínimo 8
     caracteres. Nunca se escribe en claro a disco: el asistente la guarda
     en memoria como `SecretValue` (T31.6) y el *hook* `set_user_password`
     de T36.2 la aplica con `chpasswd` leyendo `usuario:contraseña` por
     `stdin` dentro de `nixos-enter`, y la borra al terminar. No se
     introduce ninguna dependencia de *hashing* en `antosd`: para el modo
     no interactivo (`install.toml` y el smoke) se admite `password_hash`,
     que el usuario genera con `mkpasswd -m yescrypt` y T36.2 aplica con
     `chpasswd -e`.
   - Teclado: lista corta (`us`, `es`, `latam`, `de`, `fr`, `gb`, `pt-br`,
     `it`) + «otro» libre, validado contra `[a-z-]+` (T31.4). Locale y
     zona horaria: valor por defecto detectado del live (`/etc/localtime`
     y `LANG`) y editable. Todo llega a `InstallConfig` y al
     `configuration.nix` generado como opciones de `services.antos.machine`.
5. **`configuration.nix` generado** (T36.1) se reduce a: `imports`,
   `networking.hostName`, `services.antos.desktop.{enable,autologinUser}`,
   `services.antos.machine.{enable,keyboardLayout,locale,timeZone}`,
   `users.users.<u>` (sin `initialPassword`), `system.stateVersion`. El
   bloque dual-boot (`configurationLimit`, `timeout`) sigue igual.
6. **LUKS como opción declarada, no ejecutada:** `InstallConfig.encrypt:
   bool` y la pregunta en el asistente aparecen **solo** si T36.2 ya lo
   ejecuta; si no, no se pregunta. Este ticket deja el campo y el
   `boot.initrd.luks.devices` generado detrás de él, para que el ticket de
   cifrado sea solo la parte `cryptsetup`.
7. **Docs:** `docs/manual-de-comandos.md` (Método 5: `services.antos.
   machine`), la guía §4-bis (qué pregunta el asistente), y
   `docs/arquitectura.md` §5 (VM vs máquina).

## Criterios de Aceptación

- `nix eval .#nixosConfigurations.antos-desktop-x86_64.config.system.build.toplevel.drvPath`
  evalúa y su `config` tiene `networking.networkmanager.enable`,
  `services.pipewire.enable`, `hardware.bluetooth.enable`,
  `security.sudo.wheelNeedsPassword` a `true`, y **no** tiene
  `services.getty.autologinUser = "root"` ni `users.users.root.initialPassword`.
- Con `keyboardLayout = "es"`, `services.xserver.xkb.layout`,
  `XKB_DEFAULT_LAYOUT` y `console.keyMap` son coherentes (test `nix eval`
  en el job `nix-build`).
- `antos-vm` y `antos-desktop-vm` evalúan al mismo `toplevel.drvPath` que
  antes del ticket (movimiento de configuración, no cambio de
  comportamiento).
- El asistente exige contraseña (dos veces, sin eco) antes de «Disco
  Completo»/«Dual-Boot» y el sistema instalado por el smoke de T36.2 abre
  sesión con ella en TTY2 (`login` por `expect`-less: se comprueba por
  `ssh` con contraseña o `su - <user>` desde la serie).
- Ningún `initialPassword = "antos"` en el camino de máquina física
  (`grep` en CI sobre `system/nixos/machine.nix` y el generador).
- Tras instalar en QEMU con `-device virtio-sound` / `-nic user`, la sesión
  ve una tarjeta de sonido (`wpctl status`) y una interfaz gestionada por
  NetworkManager (`nmcli device`).
- `cargo test --workspace` y `cargo clippy` en verde.

## Fuera de Alcance

- Ejecutar `cryptsetup luksFormat` (ticket posterior; aquí solo la opción).
- Impresión (`services.printing`), escáneres, huella dactilar: se activan
  desde `antos-paquetes.nix` cuando el usuario los pida.
- Gestión de energía avanzada (`tlp`, `auto-cpufreq`).
- Home-manager o dotfiles por usuario: T36.5.

## Estado de Verificación (2026-09-21)

### 1. Los cambios

- **`system/nixos/machine.nix`** (nuevo, `nixosModules.machine`, en
  `nucleo`) — `services.antos.machine.{enable, keyboardLayout,
  keyboardVariant, locale, timeZone}` (tipos `strMatching`). Al activarse:
  `systemd-boot` + `efi.canTouchEfiVariables` (mkDefault), NetworkManager
  (+ el usuario del escritorio en `networkmanager`), firmware
  redistribuible, bluetooth (+ `blueman` solo con Labwc), PipeWire
  (alsa/pulse/wireplumber) + `rtkit`, teclado coherente
  (`console.keyMap` con traducción `gb→uk`, `latam→la-latin1`,
  `pt-br→br-abnt2`; `services.xserver.xkb`; `XKB_DEFAULT_LAYOUT/VARIANT`),
  `i18n.defaultLocale`, `time.timeZone`, `sudo.wheelNeedsPassword`,
  `power-profiles-daemon`, `fstrim`, `zramSwap`.
- **`system/nixos/vm-common.nix`** (nuevo) — `qemu-guest.nix`,
  `console=ttyAMA0`, `root` con sesión abierta y contraseña `antos`, y la
  contraseña de desarrollo del usuario del escritorio (solo con
  `desktop.enable`). **`configuration.nix`** se queda con
  `antos-paquetes.nix`, `services.antos.enable`, hostname y
  `stateVersion`. `base` (las VM) = `configuration.nix` + `vm-common.nix`
  + `nucleo`: mismas opciones que antes, misma máquina.
- **`desktop.nix`** — sin `initialPassword` por defecto. **`iso.nix`**
  pone `antos` para el live (RAM) y añade `mkpasswd`. **`installed.nix`**
  activa `services.antos.machine` (teclado/locale/tz con `mkDefault`),
  deja de duplicar NM/systemd-boot/zram/locale y lleva
  `initialHashedPassword = "!"` (referencia, no se instala tal cual).
  **`flake.nix`**: `antos-desktop` = `instaladaPara "aarch64-linux"` (la
  máquina física; antes heredaba la VM).
- **Generador** (`deploy/mod.rs`) — el `configuration.nix` escribe
  `services.antos.machine = { enable; keyboardLayout; locale; timeZone; }`
  y ya no duplica red/arranque/zram/locale; `validate_machine_settings`
  (teclado `[a-z][a-z-]*`, locale, zona horaria, hostname, usuario) antes
  de escribir nada: nada con comillas o `$` entra en la cadena Nix.
  `install_with_password(…, Some(&SecretValue))`: `mkpasswd -m yescrypt
  --stdin` a través del runner (la contraseña solo viaja por `stdin`; el
  `SimulatedRunner` devuelve un hash marcado) → `initialHashedPassword`.
  Precondiciones reales: sin contraseña (ni asistente ni `password_hash`)
  → error; `encrypt = true` → error explícito «no implementado» (nunca se
  ignora). `mkpasswd` en `REAL_INSTALL_TOOLS`. Las precondiciones se
  comprueban **antes** de llamar a `mkpasswd`.
- **Protocolo** — `InstallConfig.{locale, encrypt}` con `serde(default)`.
- **Asistente** (`cli.rs`) — teclado con la lista de layouts, `locale`
  (por defecto `$LANG` del live), validación, y contraseña dos veces sin
  eco (`read_hidden_line`: `libc::tcgetattr`/`tcsetattr` sobre el
  descriptor 0 solo si es terminal, eco restaurado aunque falle; sin
  `stty` ni `sh -c`), mínimo 8, tres intentos, en memoria como
  `SecretValue`; resumen sin mostrarla. TOML: `locale`, `encrypt`;
  `password_hash` obligatorio con `--apply`.
- **Smoke** — el invitado genera `password_hash` con `mkpasswd` (contraseña
  `antos`) si el TOML no lo trae; `locale` en el TOML; `mkpasswd` en el
  `path` de `antos-smoke.service`.
- **CI** (`ci.yml`) — aserciones sobre `antos-installed-x86_64`: NM,
  PipeWire, bluetooth, sudo con contraseña, systemd-boot, zram, sin
  `getty.autologinUser`, sin `initialPassword` de root ni del usuario,
  teclado coherente (xkb = `XKB_DEFAULT_LAYOUT` = `console.keyMap`),
  `gb → uk` evaluado con `--expr`, `grep` negativo de `initialPassword =
  "antos"` en `machine.nix`/`installed.nix`, y `antos-vm` sigue con root
  autologin.
- **Docs** — manual Método 5 («Máquina física vs VM de desarrollo»), guía
  §4-bis (qué pide el asistente y cómo viaja la contraseña),
  `arquitectura.md` §5.

### Decisiones

- **LUKS**: solo el campo `encrypt` (TOML/protocolo); el asistente **no**
  lo pregunta y la instalación real lo rechaza con mensaje, como pide el
  alcance (la ejecución `cryptsetup` es un ticket posterior).
- **Contraseña en el TOML**: `password_hash` (hash), nunca en claro; sin
  él `--apply` se niega. En simulación sin hash queda la contraseña de
  desarrollo marcada como «SIMULACIÓN».
- **`antos-desktop`** pasa a ser la máquina física (la instalada de
  referencia) en vez de un nombre más para la VM.

### 2. Verificación ejecutada (macOS, este entorno)

- `cargo test --workspace` → **550/550**. Nuevos: `install_with_password`
  por `mkpasswd` (comando grabado sin la contraseña, hash en el
  `configuration.nix`, precondición «sin contraseña»),
  `validate_machine_settings` (7 valores inválidos rechazados antes de
  generar), `encrypt` rechazado, sincronía generador ↔ `installed.nix`
  (incluye que ninguno duplica lo de `machine.nix` ni lleva la contraseña
  de desarrollo), `prompt_password` (corta → distinta → aceptada; tres
  fallos → cancelación; la contraseña nunca sale por pantalla), teclado
  inválido rechazado antes de pedir la contraseña, y los tests del
  asistente con los prompts nuevos.
- `cargo clippy` (ambas puertas) y `cargo fmt --check` limpios.
- Asistente con el binario (dry-run, entrada por tubería): pide teclado,
  idioma y contraseña (rechaza «corta», acepta la segunda), y el
  `configuration.nix` lleva `services.antos.machine` con `es` /
  `es_ES.UTF-8` / `Europe/Madrid` e `initialHashedPassword` (hash
  simulado).
- YAML de `ci.yml` validado.
- **No verificable aquí** (sin Nix): la evaluación de `machine.nix`, las
  aserciones de CI, que `antos-vm`/`antos-desktop-vm` conserven el
  `toplevel.drvPath` (por construcción: `vm-common.nix` es el mismo
  contenido movido, y la contraseña del usuario sigue siendo `antos` vía
  `mkDefault`), y el sonido/red en QEMU (smoke, ISO de T36.3).

### Criterios de aceptación — estado

- `antos-desktop-x86_64` evalúa con NM/PipeWire/bluetooth/sudo y sin root
  autologin: **⏳ aserciones en CI sobre `antos-installed-x86_64`** (el
  nombre `antos-desktop` es la variante aarch64 de la misma máquina).
- Teclado `es` coherente (xkb, `XKB_DEFAULT_LAYOUT`, `console.keyMap`):
  **⏳ aserción en CI** (+ `gb → uk`).
- `antos-vm` y `antos-desktop-vm` con el mismo `drvPath`: **⏳ por
  construcción; se confirma comparando con el commit anterior en CI si se
  quiere** (la CI comprueba al menos que `antos-vm` conserva el autologin
  de root).
- El asistente exige contraseña dos veces sin eco: **✅** (tests; el
  «login en TTY2» del smoke depende de la ISO).
- Sin `initialPassword = "antos"` en la máquina física: **✅** (`grep` en
  CI y test de sincronía).
- Sonido y NetworkManager visibles tras instalar en QEMU: **⏳ smoke**.
- `cargo test --workspace` y `cargo clippy`: **✅**.

### Verificación real en la podman-machine (2026-09-22)

Como GitHub Actions no ejecuta en este repositorio (ver T31.10), lo que
la CI habría hecho se ejecutó a mano en el contenedor `nixos/nix` de la
`podman-machine` (aarch64-linux, 5 CPU, 7,4 GiB), sobre `git+file:///src`:

- `antos-installed-aarch64` evalúa con `services.antos.machine.enable`,
  NetworkManager, PipeWire, bluetooth, `sudo` con contraseña,
  `systemd-boot`, `zram`, **sin** `getty.autologinUser` ni contraseña de
  root ni del usuario, y teclado coherente (`xkb.layout` =
  `XKB_DEFAULT_LAYOUT` = `console.keyMap`). `antos-vm` conserva el
  autologin de root. **Hallazgo y corrección:** `vm-common.nix` ponía el
  `mkIf` dentro del valor de `users.users.<u>.initialPassword`, lo que
  definía al usuario `antos` (sin `isNormalUser`) también en la VM
  headless y rompía `nixosConfigurations.antos`; el `mkIf` va ahora en
  `users.users` (con `mkMerge` junto a root).
- Criterios de evaluación Nix: **✅ verificados** (la coherencia `gb → uk`
  la comprueba la CI; el sonido/red en QEMU sigue en el smoke).
