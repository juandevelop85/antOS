name = "env.sync"
summary = "Sincroniza y verifica las toolchains de desarrollo declaradas en el workspace"

[params]

[effects]
reads = ["$WORKSPACE/.antos/env.toml", "$WORKSPACE/devbox.json", "$WORKSPACE/flake.nix"]

[policy]
tier = "auto"
reversible = "unnecessary"
