name = "secret.revoke"
summary = "Revoca inmediatamente el acceso temporal a un secreto o archivo sensible"

[params]
secret = { type = "slug", max = 32 }

[effects]
writes = ["$STATE/grants.json"]

[policy]
tier = "confirm"
reversible = "never"
