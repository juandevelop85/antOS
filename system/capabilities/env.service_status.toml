name = "env.service_status"
summary = "Consulta el estado de los servicios locales efímeros aprovisionados"

[params]
service = { type = "slug", max = 32, optional = true }

[effects]
writes = []

[policy]
tier = "auto"
reversible = "unnecessary"
