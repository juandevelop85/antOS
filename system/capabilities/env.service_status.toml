name = "env.service_status"
summary = "Estado real de los servicios locales registrados: re-sondea el puerto y comprueba el PID (origen, salud, variable inyectada)"

[params]
service = { type = "slug", max = 32, optional = true }

[effects]
writes = []

[policy]
tier = "auto"
reversible = "unnecessary"
