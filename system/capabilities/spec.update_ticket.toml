name = "spec.update_ticket"
summary = "Actualiza el estado de un ticket técnico en el catálogo del proyecto"

[params]
ticket_id = { type = "text" }
status = { type = "text" }

[effects]
writes = ["$WORKSPACE/docs/tickets/"]

[policy]
tier = "confirm"
reversible = "snapshot"
