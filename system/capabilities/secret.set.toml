name = "secret.set"
summary = "Guarda de forma segura una variable secreta o clave en la bóveda de antOS"

[params]
key = { type = "slug", max = 64 }
value = { type = "slug", max = 512 }

[effects]
writes = ["$STATE/vault.json"]

[policy]
tier = "confirm"
reversible = "never"
