name = "env.service_down"
summary = "Detiene y limpia un servicio auxiliar efímero de desarrollo"

[params]
service = { type = "slug", max = 32 }

[effects]
writes = ["$STATE/services/{service}/"]

[policy]
tier = "confirm"
reversible = "never"
